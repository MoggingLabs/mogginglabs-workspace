# Findings ledger

Every finding from the two audit passes, with the status it holds at HEAD. **This file is gated** by `scripts/check-findings-ledger.mjs` (sweep row `LEDGER`): a row may not lose its status, a `deferred` row must carry a reason, and the row count is pinned so findings cannot silently vanish.

Evidence: [`2026-08-01-full-feature-audit.md`](./2026-08-01-full-feature-audit.md) - the original 375-finding audit. [`2026-08-01-audit-revalidation.md`](./2026-08-01-audit-revalidation.md) - re-validation at HEAD plus the new-code findings.

## Status vocabulary

| status | meaning |
| --- | --- |
| `open` | present at HEAD; needs work |
| `fixed` | closed, with the guard that closed it named in the evidence doc |
| `invalid` | re-reading showed the claim was wrong, or verification refuted it |
| `deferred` | real, consciously not being done - **requires a reason of 20+ chars** |

## Totals

| | count |
| --- | --- |
| rows | 497 |
| open | 488 |
| fixed | 2 |
| invalid | 7 |
| deferred | 0 |

| severity critical | 7 |
| severity high | 79 |
| severity medium | 280 |
| severity low | 127 |
| severity none | 4 |

## Rows

| id | sev | status | location | note / next action | reason (deferred only) |
| --- | --- | --- | --- | --- | --- |
| connection-stdio-bridge-into-agent-clis-adr-0014/F1 | critical | `open` | `src/main/cli-runtime.ts:153` | Unchanged. The v11 bump does not alter the redirect (any vN matches). bin/mogging-mcp.mjs still has no --connection handling, so the flag is silently ignored. |  |
| connection-stdio-bridge-into-agent-clis-adr-0014/F2 | critical | `open` | `src/main/cli-runtime.ts:203` | Raised from HIGH: fb72bef bumped DAEMON_PROTOCOL_VERSION 10->11 (contracts/daemon/protocol.ts:38), so this release actually strands existing entries on the swep |  |
| delta/conpty-v2-and-pin/2 | critical | `invalid` | `src/pty-daemon/session.ts:1026` | refuted by verification: Bare kill at session.ts:1026 is real, but the DLL branch isn't sweep-less: conpty.cc:534-566 PtyKill = ConptyClosePseudo |  |
| delta/daemon-protocol-v11/10 | critical | `fixed` | `src/ui/features/terminal/terminal-pane.ts:529` | After the reconnect replay resolves, push the new gen to the renderer (a gen-refresh IPC the pane applies to sessionGen), or re-stamp in the relay's write/resiz |  |
| delta/install-engine-has-zero-executed-coverag/110 | critical | `fixed` | `src/backend/platform/env-path.ts:383` | Return the reg exit code from run() and require a successful HKCU query before writing; when the read failed, return {ok:false,error} instead of calling `reg ad |  |
| delta/persistuserpathentries-can-wipe-hkcu-pat/117 | critical | `fixed` | `src/backend/platform/env-path.ts:383` | Make run() return {ok, code, stdout}; in persistWindows distinguish reg's "value not found" (exit 1) from any other failure/timeout and write nothing on the lat |  |
| delta/terminal-dims-restore/16 | critical | `fixed` | `src/ui/features/terminal/terminal-pane.ts:529` | In daemon-relay's write/resize handlers stamp the gen main already tracks (`gens.get(id)` when numeric) instead of forwarding cmd.gen, or push a gen-changed IPC |  |
| a11y/F1 | high | `open` | `src/ui/features/workspace/controller.ts:550` | Code and severity unchanged; the baseline diff of controller.ts is one unrelated remote-cwd hunk at :499. Auto-collapse means the user need not opt in to reach |  |
| a11y/F3 | high | `open` | `src/ui/features/terminal/terminal-pane.ts:170` | Holds. The delta arguably widens the gap: ConPTY v2 and replay-integrity work invested heavily in pane content correctness while that content stays unexposed to |  |
| accounts-entitlements/F1 | high | `open` | `src/main/account.ts:592` | Unchanged by the delta. High holds: the two neighbouring branches in the same function make the omission starker - chip trouble and AS trouble keep the session, |  |
| agent-web-consent/F1 | high | `open` | `src/main/app-settings.ts:68 · src/main/browser-dock.ts:154` | Trap: readGrant's legacy migration (grant-store.ts:101) would re-open web:'public' from a surviving consent row if the grant row were deleted. |  |
| brain/F1 | high | `open` | `src/backend/features/brain/freshness.ts:411` | Severity unchanged; delta touched neither file. Both triggers (branch switch, app-closed lockfile edit) remain open — list_libraries/get_library_docs serve stal |  |
| browser-dock/F2 | high | `open` | `src/main/browser-dock.ts:551` | High stands: the Stop button lives inside the strobing banner, so the user's only revocation control is unhittable during a verb sequence. The 559-561 comment a |  |
| ci-workflows/F1 | high | `open` | `scripts/check-sweep-log.sh:10` | Unchanged. Violates gate honesty (a gate that never ran certifies green) and win/mac parity. The ci.yml delta was only 199->207 comment bumps plus two weight-ga |  |
| command-blocks/F1 | high | `open` | `src/ui/features/terminal/pane-anchor.ts:226 (jump call now terminal-pane.ts:880-882)` | Lines drifted (216->226, 836->880) from the +10-line sync-output guards and unrelated pane edits; the defect is byte-identical. |  |
| command-blocks/F2 | high | `open` | `src/ui/features/blocks/block-tracker.ts:149` | As originally recommended: wire a palette command or per-pane find bar calling find()+jumpTo(), and cover it in the smoke. |  |
| connection-stdio-bridge-into-agent-clis-adr-0014/F3 | high | `open` | `src/main/connections.ts:1266` | Unchanged. The comment at connections.ts:1259-1260 ('The shim sets ELECTRON_RUN_AS_NODE itself') is now false - ADR 0017 removed it, cliShimSource sets nothing. |  |
| connection-stdio-bridge-into-agent-clis-adr-0014/F5 | high | `open` | `scripts/connections-pure-smoke.ts:369` | Raised from MEDIUM on gate honesty: the delta shipped a protocol bump (making F2 live) plus 10 new gates through the same green board, while the composition age |  |
| connections-oauth/F1 | high | `open` | `src/main/connections.ts:1402` | Unchanged severity. The delta shipped the device route, so more cards rest on app-held grants. RefreshCoordinator (credential-core.ts:123-185) has no offline br |  |
| curated-rest-tool-bridge-adr-0020-0021/F1 | high | `open` | `src/backend/features/integrations/rest-bridge.ts:275` | posthog.json's pageParam is equally unread but PostHog does send a next URL, so those work by accident; Stripe's 4 tools are the real break. restexec-pure-smoke |  |
| delta/agent-setup-onboarding/31 | high | `open` | `src/backend/features/agents/setup.ts:287` | Do not gate winget/python on a stat. Probe by spawning (`winget --version`, exit 0) or resolve App Execution Aliases explicitly; keep resolveOnPath for real fil |  |
| delta/agent-setup-onboarding/32 | high | `open` | `src/pty-daemon/session.ts:538` | Build inheritedEnv with mergeEnv(process.env, {AIDER_ANALYTICS_LOG}, extraEnv, spec.env) instead of the object spread at session.ts:532-539, so the overlay's PA |  |
| delta/agent-setup-onboarding/33 | high | `open` | `src/backend/features/agents/setup.ts:170` | Install into a venv or pipx the app owns (or pass --user), re-run applyLivePathToProcess between install steps, and add pip script dirs (~/Library/Python/*/bin, |  |
| delta/agent-setup-onboarding/34 | high | `open` | `src/backend/features/agents/setup.ts:395` | Only remember dirs the run actually created (drop rememberBinDir from the writable branch), and give persistPosix the same already-covered check persistWindows |  |
| delta/conpty-v2-and-pin/3 | high | `open` | `scripts/rebuild-native.mjs:71` | Append `node scripts/build-node-helper.mjs` after install-app-deps in rebuild-native.mjs, and add build/Release/conpty/conpty.dll to its post-rebuild existence |  |
| delta/conpty-v2-and-pin/4 | high | `open` | `scripts/check-conpty-pin.mjs:63` | On win32 require checked > 0 and assert the pair EXISTS wherever a node-pty root exists (fail on node-pty with no conpty/ dir); off win32 print SKIPPED rather t |  |
| delta/conpty-v2-and-pin/5 | high | `open` | `src/backend/platform/pty-host.ts:107` | Carry the conpty backend choice in the daemon endpoint stamp and retire/respawn when it differs from the app's current choice (same machinery as the build-stamp |  |
| delta/contracts-ipc-delta/95 | high | `open` | `src/contracts/integrations/first-party-clients.ts:88` | Gate the override behind an unpackaged/dev check at connections.ts:455 and client-registry.ts:100, and add the `MOGGING_OAUTH_CLIENT_` prefix to check-prod-arti |  |
| delta/daemon-protocol-v11/11 | high | `open` | `src/main/smokes/daemonheal-smoke.ts:90` | Add a renderer-path arm to DAEMONHEAL or SURVIVE: spawn a pane through the IPC handlers, kill the daemon, wait for the heal, then send TerminalChannels.write ca |  |
| delta/daemon-protocol-v11/12 | high | `open` | `src/ui/features/terminal/pane-drop.ts:179` | Route every renderer write through one helper that stamps the owning pane's sessionGen (expose it via the pane registry for pane-drop, the typeIntoPane port and |  |
| delta/defaults-tier/40 | high | `open` | `src/backend/features/agent-settings/service.ts:580` | In setAccountDefault, before fan-out, adopt any legacy (tier-null) override for that settingId+surface at the authoring home by deleting it so fan-out compiles |  |
| delta/design-system-css/79 | high | `open` | `src/ui/styles/global.css:3090` | Add a per-theme --info-ink (the precedent --danger-ink sets at global.css:110-118) for .pane-signin-action / svg / -text; keep --info for border and tint only. |  |
| delta/explorer-sync-laws/54 | high | `open` | `src/ui/features/terminal/pane-drop.ts:171` | Delete the fallback branch — dragstart and drop ship in one renderer, so 'an older drag' cannot occur — or route quotedFallback through quotePathsForShell first |  |
| delta/gate-honesty-delta/86 | high | `open` | `scripts/check-conpty-pin.mjs:80` | Refuse `checked === 0` on win32 with exit 1 ("no staged conpty pair — the overlay never ran"), and off win32 print `CONPTYPIN SKIPPED (not a windows host)` inst |  |
| delta/gate-honesty-delta/88 | high | `invalid` | `src/main/smokes/fileact-smoke.ts:124` | refuted by verification: FILEACT never pastes, so sanitizePaste never reaches this wire. pane-insert.ts:39 and pane-drop.ts:174 pad inserts with |  |
| delta/gate-honesty-delta/90 | high | `invalid` | `.github/workflows/release.yml:229` | refuted by verification: "win/mac only on manual dispatch" is false: windows-sweep (ci.yml:412, cron '30 5 * * *') and macos-sweep (ci.yml:328, c |  |
| delta/install-engine-has-zero-executed-coverag/111 | high | `open` | `src/backend/platform/env-path.ts:435` | In persistPosix, reuse loginShellPath() to drop entries already on the shell's PATH before writing, and change rcBlock to append (`export PATH="$PATH":"<dir>"` |  |
| delta/install-engine-has-zero-executed-coverag/112 | high | `open` | `src/backend/features/agents/setup.ts:170` | For spec.requires==='python', install into an app-owned venv (python -m venv, then <venv>/bin/pip) and rememberBinDir that venv's bin; or pass --user and stop a |  |
| delta/installer-freeze-fixes/65 | high | `open` | `build/installer.nsh:102` | Move the retire + SetDetailsPrint work into a macro also expanded from customInstall (installSection.nsh:84 runs unconditionally), or add a customInit-side reti |  |
| delta/oauth-device-flow/47 | high | `open` | `src/backend/features/integrations/oauth.ts:681` | Treat `attempt.errorCode === undefined` (transport failure, no OAuth error body) as retryable: keep looping until grant.expiresAt, optionally after N consecutiv |  |
| delta/oauth-device-flow/48 | high | `open` | `src/main/connections.ts:1156` | In doRefresh, fall back to `firstPartyClientFor(disco.issuer, process.env)` (same shape as connections.ts:455-461) before declaring 'The client registration is |  |
| delta/persistuserpathentries-can-wipe-hkcu-pat/118 | high | `open` | `src/backend/platform/env-path.ts:147` | Read PATH with an interactive-login invocation (`-ilc`) for zsh/bash behind the existing timeout, or move the persisted block to ~/.zprofile so the read half an |  |
| delta/persistuserpathentries-can-wipe-hkcu-pat/119 | high | `open` | `src/backend/platform/env-path.ts:415` | Make rcBlock append (`export PATH="$PATH":"<dir>"`, `fish_add_path -ga <dir>`) so the new bin dir gets the same precedence as on Windows — or make persistWindow |  |
| delta/persistuserpathentries-can-wipe-hkcu-pat/120 | high | `open` | `src/backend/platform/env-path.ts:168` | Add `join(home, '.npm-global')` and `join(home, '.npm-global', 'bin')` to the win32 candidates in wellKnownBinDirs, so the directory this app itself creates is |  |
| delta/remote-insert-honesty/60 | high | `open` | `src/ui/features/terminal/pane-drop.ts:171` | Route both drop branches through typeIntoPane (or apply the same control-char strip), and refuse a fallback payload matching /[\x00-\x1f\x7f]/. Change remote-sm |  |
| delta/remote-insert-honesty/61 | high | `open` | `src/pty-daemon/session.ts:538` | Build inheritedEnv with mergeEnv(process.env, {AIDER_ANALYTICS_LOG}, extraEnv, spec.env) instead of the spread, and add a gate arm that spawns a pane after a PA |  |
| delta/terminal-audit-fixes/22 | high | `open` | `src/backend/features/terminal/pane-shared.ts:61` | Drop the cursor-moving members: use \x1b[?1047l instead of ?1049l, and if the scroll region must be reset, follow \x1b[r with an explicit reposition (\x1b[999;1 |  |
| delta/terminal-audit-fixes/23 | high | `open` | `src/main/daemon-relay.ts:278` | Do not drop the ring on a flap: either reset-and-repaint on both branches, or have the daemon return only the bytes appended since the client's last acknowledge |  |
| delta/terminal-audit-fixes/25 | high | `invalid` | `src/ui/features/terminal/terminal-pane.ts:125` | refuted by verification: Guard missed: terminal-pane.ts:558 re-arms before the replay is parsed. xterm defers term.write (setTimeout _innerWrite, |  |
| delta/terminal-dims-restore/17 | high | `open` | `src/main/smokes/restoredims-smoke.ts:97` | Neutralize platform-independently: spawn the smoke pane with an explicit non-login shell (spec.args: []), or type a command name that exists on no machine (e.g. |  |
| delta/terminal-dims-restore/18 | high | `open` | `src/main/smokes/restoredims-smoke.ts:24` | Add a renderer-side assertion (PANEFIT already drives a hidden second workspace) that a pane mounted under display:none records a spawn request with cols/rows a |  |
| delta/two-adrs-both-numbered-0022/124 | high | `open` | `src/backend/features/agent-settings/service.ts:519` | Return ok:true plus a per-home warning from applyAccountDefaults when at least one home applied, and in agent-config.ts save() always call load(currentSnapshot. |  |
| delta/two-adrs-both-numbered-0022/125 | high | `open` | `src/ui/features/settings/agent-config.ts:428` | Carry a display label for the resolved primary home in the snapshot (no raw path, so noPathLeak still holds) and render it in consentCrossAccount; change defaul |  |
| delta/wizard-redesign/73 | high | `open` | `src/ui/styles/global.css:4773` | Route tryLaunch's refuse() through showLaunchAlert(text) as launch()'s refuse() already does (index.ts:505-509), so a refused Launch always has a visible surfac |  |
| delta/worktree-preflight-split-git-timeouts/103 | high | `open` | `src/backend/features/worktrees/index.ts:87` | Probe writability without persisting: mkdtempSync under repo then rmSync, or mkdir then rmdirSync back the levels this call created (rmdir refuses non-empty). I |  |
| delta/worktree-preflight-split-git-timeouts/104 | high | `open` | `src/backend/features/worktrees/index.ts:218` | Pass CHECKOUT_MS to the `worktree remove` call in removeWorktree (deleting a checkout costs what writing it cost); keep the `status --porcelain` probe on QUICK_ |  |
| deps-hygiene/F1 | high | `open` | `scripts/build-node-helper.mjs:243` | Unchanged severity; delta sharpened it. prune-helper-deps.mjs:16-26 documents bindings->file-uri-to-path as load-bearing in the shipped tree. The new WEIGHT gat |  |
| event-bridge/F1 | high | `open` | `src/pty-daemon/transport.ts:231` | transport/session changed only for gen-gating and remote cwd; event-bridge.ts is byte-identical to c026463. docs/14:470-471 still promises notify 'rings the flo |  |
| explorer-files/F2 | high | `open` | `src/ui/features/explorer/index.ts:584` | Raised from the verifier's medium: it does NOT self-heal. lastFiles clears only in unwatchFiles (monitor.ts:131) and refreshFiles is change-only (:322), so reop |  |
| local-endpoint-token-custody-and-lifecycle/F1 | high | `fixed` | `src/main/mcp-endpoint.ts:394` | mcp-endpoint.ts has zero diff vs c026463 — the 32-commit delta never touched this file. Severity unchanged: no-token local heap exhaustion of main (docs/05 heap |  |
| local-endpoint-token-custody-and-lifecycle/F2 | high | `open` | `src/main/mcp-endpoint.ts:196` | Zero diff in the file; bin/mogging-connection.mjs:56 still says 'The MCP proxy path never reads it.' Blast radius grew — OAuth device flow landed (oauth.ts +262 |  |
| mcp-registration/F1 | high | `open` | `src/main/tool-plan.ts:171` | Severity holds. Reinforced: the delta added a linked-worktree preflight feature (tests/unit/worktree-preflight.test.ts), so scoped launches inside .mogging/work |  |
| per-pane-git-branch-dirty-chip-pipeline/F4 | high | `open` | `src/main/smokes/git-smoke.ts:250` | RAISED medium->high. Not a plain test gap: the leg is dead-on-arrival (rejected authority) AND unasserted, so GIT certifies green over a leg that cannot run — t |  |
| perf-and-perception-budgets-vs-shipped-smokes/F1 | high | `fixed` | `src/main/smokes/perception-smoke.ts:167` | Checked if the delta worsened it: v11 gen-gates input but transport.ts:214 accepts genless and daemon-relay.ts:447 forwards undefined gen, so the raw write stil |  |
| perf-and-perception-budgets-vs-shipped-smokes/F2 | high | `open` | `src/main/smokes/milestone-smoke.ts:168` | More load-bearing now: ConPTY v2, protocol v11 and replay/restore work all land inside the 16-pane torrent window nothing gates. docs/05:36 records it as 'frame |  |
| persistence-restore/F1 | high | `open` | `src/main/app-settings.ts:32-36` | The delta added board/shared-defaults/agent-config state into this same db (settings-store.ts), so one corruption event now bricks even more surface — severity |  |
| profiles-failover/F1 | high | `open` | `src/ui/features/agents/index.ts:468` | index.ts changed only for the sign-in banner; liveness-port.ts untouched. Nothing clears the reattach mark on agent exit. profiles-smoke.ts:109-124 exercises fa |  |
| profiles-failover/F2 | high | `open` | `src/ui/features/agents/index.ts:682` | Also a 'we type, the user executes' breach: when the CLI survives one ^C the app presses Enter on a chat message into the capped account. The smoke passes only |  |
| pty-daemon/F1 | high | `fixed` | `src/pty-daemon/session.ts:1026` | Line drifted 923->1026 (deferred-launch code landed above). Vendored node-pty under useConptyDll (now default) still skips console-list kill, so headless orphan |  |
| pty-daemon/F4 | high | `open` | `src/pty-daemon/transport.ts:227` | Lines drifted (222->224/227) from the v11 gen-gate blocks. The v11 optional gen field gates staleness, not identity — it does not close forged sibling notify or |  |
| qa-gates-integrity/F1 | high | `open` | `scripts/check-sweep-log.sh:10 · .github/workflows/ci.yml:305,389` | No commit since c026463 touched check-sweep-log.sh's regex. |  |
| remotes-ssh/F1 | high | `open` | `src/ui/features/terminal/terminal-pane.ts:591-606` | Unchanged. The delta touched terminal-pane.ts (gen stamping, OSC 52 grace, measured dims) but never the readiness-latch lifecycle. High stands: this is the cred |  |
| remotes-ssh/F2 | high | `open` | `src/pty-daemon/session.ts:480 (seed); :901 (snapshot)` | Slightly likelier now: a cold-start restore spawns at the persisted grid and the resume is deferred until dims are confirmed (deferLaunch/confirmDims) — it fire |  |
| review-merge-gate/F1 | high | `fixed` | `src/backend/features/review/redact.ts:41` | File unchanged since baseline (git diff c026463..HEAD empty). Real credentials still reach the renderer and the copy-hunks clipboard (ui/features/review/index.t |  |
| security-hardening/F1 | high | `fixed` | `src/main/libfetch.ts:29` | Delta touched none of libfetch.ts, origins.ts, check-originpin.mjs. Severity unchanged: fetched text lands in agent context; ADR 0016 §6 forbids env-readable or |  |
| service-adapters-github/F1 | high | `open` | `src/backend/features/integrations/services/engine.ts:163` | Untouched by the delta (no diff in engine.ts/services.ts since c026463); severity unchanged. |  |
| telemetry/F1 | high | `fixed` | `src/main/telemetry.ts:38` | Unchanged. Renderer comment at telemetry.ts:53 asserts 'main routes to PostHog only while product analytics is consented' — main does no such check, which is wh |  |
| telemetry/F2 | high | `open` | `src/main/sentry-telemetry.ts:31` | Unchanged. The delta widened exposure: one-click agent setup added agents/install.ts:74 captureError(op:'install-spawn') — spawn/ENOENT messages are exactly the |  |
| telemetry/F3 | high | `open` | `src/main/sentry-telemetry.ts:26` | Unchanged, but the delta made it concrete: boot.ts:267 now console.warns '[env] PATH repaired — the launch environment was missing ' + dirs — a console breadcru |  |
| templates/F1 | high | `open` | `src/backend/features/workspace/settings-store.ts:898 (was :777); src/main/templates.ts:30` | File grew ~121 lines since baseline, but the 06b template block is byte-identical; only line numbers shifted. |  |
| terminal-rendering/F1 | high | `open` | `src/ui/features/terminal/pane-anchor.ts:227` | The delta's mid-frame defer makes the yank land one frame later during TUI repaints but does not prevent it. Keyboard scrollback paging and shift-click history |  |
| updater-distribution/F2 | high | `open` | `.github/workflows/release.yml:332` | Unchanged. Still the highest-impact item here: one dispatch re-run against an old tag repoints /releases/latest for every install; prerelease users downgrade. |  |
| usage-metering/F1 | high | `open` | `src/backend/features/usage/claude-adapter.ts:250` | Zero changes to claude-adapter.ts or the boot schedule in the delta; ADR 0007:52-54 still says 'never speculatively at boot'. Severity held per prior verifier: |  |
| window-chrome-clipboard/F1 | high | `open` | `src/main/clipboard.ts:371` | Unchanged. Rests on Windows reading LF-written text back as CRLF — asserted in the repo's own write-path comment (:289-294), never proven, which is exactly why |  |
| winget-homebrew-install-manifests/F1 | high | `open` | `.github/workflows/release.yml:245` | Delta added check-package-weight.mjs (ci.yml:219, 536) — guards bundle contents, not hashes. Severity held: manifests still unsubmitted (docs/10:13), so impact |  |
| worktrees/F3 | high | `open` | `src/backend/features/worktrees/index.ts:94` | Unchanged; a Windows-only failure after a green preflight — platform-divergence law keeps this high. |  |
| a11y/F2 | medium | `open` | `src/ui/features/workspace/controller.ts:749` | Unchanged. Now provably compounds F1: the click path forces `activate.focus({ preventScroll: true })` (controller.ts:606), so the focused element has neither na |  |
| a11y/F4 | medium | `open` | `src/ui/components/toast.ts:103` | Unchanged; TOAST_DEFAULT_MS still 6000 (toast.ts:21). Custody exposure grew: the delta added OAuth device flow and shared-defaults, both surfacing save-time ref |  |
| accounts-entitlements/F2 | medium | `open` | `src/main/entitlements.ts:272` | Unchanged by the delta. account.ts clearSession (:606 path) still leaves VAULT_CACHE intact. Medium holds - needs the two-users-on-one-machine sequence plus a f |  |
| accounts-entitlements/F3 | medium | `open` | `src/main/entitlements.ts:397` | Unchanged by the delta. Medium holds: an issuer-dependent latent cliff, not a defect on well-behaved TTLs. |  |
| accounts-entitlements/F4 | medium | `open` | `src/main/entitlements.ts:220` | Unchanged, but starker: the delta added ~25 test/smoke files (+2528 lines, whole new suites) while this security-relevant invariant still has no test at any lev |  |
| agent-launcher/F2 | medium | `open` | `src/backend/features/agents/setup.ts:112` | New tests/unit/spawn-tool.test.ts covers quoting only, not kill semantics. killPtyTree exists (install.ts:87) but SetupService never uses it — the tree-kill pat |  |
| agent-launcher/F3 | medium | `open` | `src/main/agents.ts:135` | Same code, same call-site line. Main now also relays gen-gated daemon v11 pty traffic, so a long block at failover launch still violates the docs/05 frame-gap b |  |
| agent-launcher/F4 | medium | `open` | `src/backend/features/agents/launch.ts:49` | Windows/macOS parity divergence persists; the measured fix remains one import away as originally stated. |  |
| agent-launcher/F5 | medium | `open` | `src/backend/features/agents/launch.ts:102` | The untested surface grew: buildLaunchCommand now takes LaunchTargetSpec for saved remote hosts (launch.ts:108) and remote-insert honesty routes remote launches |  |
| agent-launcher/F6 | medium | `open` | `src/backend/features/agents/logins.ts:54` | Platform-parity violation unchanged: a signed-in macOS user whose .claude.json lacks oauthAccount is offered sign-in; Windows/Linux read the same state correctl |  |
| agent-settings-editor/F1 | medium | `open` | `src/main/agent-settings.ts:460` | Stays at the prior verifier's corrected medium. Delta widens it: reconcileAll (service.ts:381-383, filters only ownership==='enforce') now also walks ADR 0022 t |  |
| agent-settings-editor/F2 | medium | `open` | `src/backend/features/agent-settings/service.ts:300` | Delta duplicated the pattern into the ADR 0022 fan-out: applyAccountDefaults reads its own baseline at service.ts:591-594, reconciles at 627. That path runs aut |  |
| agent-settings-editor/F3 | medium | `open` | `src/backend/features/agent-settings/sources.ts:245` | Unchanged by the delta, slightly worse in practice: applyAccountDefaults writes through the same selectAgentConfigSource (service.ts:586), so account-defaults a |  |
| agent-settings-editor/F4 | medium | `open` | `src/backend/features/agent-settings/service.ts:186` | Unchanged by the delta, but the rollup at 185 lists rows without a tier filter, so drift on an ADR 0022 compiled account-default is equally invisible in the ove |  |
| agent-state-attention/F1 | medium | `open` | `src/backend/features/terminal/pty.service.ts:156-161` | Original cited src/backend/boot.ts:300; the unchanged 'work normally' message actually lives at src/main/boot.ts:300. Everything else verified byte-identical si |  |
| agent-state-attention/F2 | medium | `open` | `src/backend/features/agent-state/activity.ts:255` | as originally recommended: keep deferredDone intact while pendingSubagents > 0, plus an attention-smoke assert that busy-during-fan-out still lets the last suba |  |
| agent-web-consent/F2 | medium | `open` | `src/main/browser-dock.ts:674` | beginDriving (line 526) feeds only the volatile in-memory activity list, not recordTrail. |  |
| agent-web-consent/F3 | medium | `open` | `src/main/browser-dock.ts:673` | MCP writes still record pane, so the asymmetry with mcp-endpoint.ts persists. |  |
| agent-web-consent/F4 | medium | `open` | `src/main/browser-dock.ts:692` | Contract still documents 'confirmed' = performed after confirm (contracts/integrations/trail.ts:15). |  |
| agent-web-consent/F5 | medium | `open` | `src/contracts/integrations/grant.ts:71` | 'bank' does catch bankofamerica.com; the gap is non-'bank'-named institutions. |  |
| agent-web-consent/F6 | medium | `open` | `src/main/browser-dock.ts:675` | Window is narrow (page-initiated navigation committing between gate and inject) but it is exactly the boundary the per-origin grant holds. |  |
| agent-web-consent/F7 | medium | `open` | `tests/unit/ (no grant-store/trail test) · grant-store.ts:63` | The 32-commit window added unit tests in other areas but none here. |  |
| board-kanban/F1 | medium | `open` | `src/ui/features/board/launch.ts:155` | Held at the prior verifier's medium, not the original HIGH: notes with blank lines submitting fragment-by-fragment is common but harmless; injection needs hosti |  |
| board-kanban/F3 | medium | `open` | `src/ui/features/board/queue.ts:90-93` | Unchanged. Both docs/18 directions stay exposed: a lost `enabled:false` relaunches unattended agents; a clobbered `launches` under-counts an engine-enforced bud |  |
| board-kanban/F4 | medium | `open` | `src/ui/features/board/index.ts:242-249` | Unchanged. Still violates docs/18 'a dead pane's claim never blocks anyone'. Chips/liveBusy (queue.ts:47,205) self-correct via paneInstance, but the main-side c |  |
| brain/F2 | medium | `open` | `src/ui/features/terminal/terminal-pane.ts:970` | Severity unchanged. Ordering hazard now explicit: boot.ts:430's before-quit calls disposeBrain() synchronously, so any renderer-round-trip flush would race the |  |
| brain/F3 | medium | `open` | `tests/unit/brain-libraries.test.ts:1` | Severity unchanged, but the gap is more conspicuous: the delta added eight unit-test files for other areas while the brain's byte-splice and memory-parse invari |  |
| browser-dock/F1 | medium | `open` | `src/ui/features/browser/index.ts:421` | Held at the prior verifier's corrected medium (orig HIGH): a crashed guest is a white rectangle with no Retry, but manual Reload recovers. Zero browser files ap |  |
| browser-dock/F3 | medium | `open` | `src/main/browser-dock.ts:959` | Medium stands. Defense-in-depth, but the attach→dom-ready gap it was meant to close is exactly what docs/13:130-134 promises is closed, and Electron grants perm |  |
| browser-dock/F4 | medium | `open` | `src/main/browser-dock.ts:512` | Medium stands. Two stale states persist: a tab dot claiming a departed driver, and permanent exemption from the GUEST_CAP=3 LRU (index.ts:308,491-497) — so a la |  |
| browser-dock/F6 | medium | `open` | `src/ui/features/browser/index.ts:533` | Medium stands. Electron ships no popup blocker, so a hostile/buggy page in any live guest can loop window.open into unbounded renderers, blowing docs/05 budgets |  |
| ci-workflows/F2 | medium | `open` | `.github/workflows/release.yml:229` | The delta added WEIGHT in two new places (ci.yml:223-224 per-push linux, :538-542 dispatch) but not the release path. Linux is now shape-covered per push; Windo |  |
| ci-workflows/F3 | medium | `open` | `.github/workflows/ci.yml:183` | Unchanged. The key's own comment (ci.yml:176-178) still promises 'a source edit can never serve a stale artifact' — false for the one file deciding what ships i |  |
| ci-workflows/F4 | medium | `open` | `scripts/kill-devservers.mjs:76` | Severity unchanged; blast radius grew — the sweep is now 207 gates (qa-smokes.sh:12) and kill_devserver runs after each of ~175 boot smokes (:161). The SAFETY c |  |
| ci-workflows/F5 | medium | `open` | `.github/workflows/ci.yml:150` | The delta widened the gap: a37ff5e added the per-push weight gate to linux-boot only (:223-224), so Windows — whose NSIS double-write is WEIGHT's stated motivat |  |
| cli-ux/F1 | medium | `open` | `bin/mogging.mjs:62` | Held; bin/ untouched by the delta. Note the F3 interaction: the command docs/09 gives remote operators (`mogging endpoint --path`) is itself unrecognized, so F1 |  |
| cli-ux/F2 | medium | `open` | `bin/mogging.mjs:32` | Held. Same class also hits --no-enter: L1115 filters it from anywhere in argv, so that literal can never be typed. Protocol v11's gen is optional on the input f |  |
| cli-ux/F3 | medium | `open` | `docs/09-swarm.md:113` | Held. The value the verb would print already exists: endpointFilePath() at bin/mogging.mjs:821 returns MOGGING_DAEMON_ENDPOINT or runFile(RUN_SEGMENT,'endpoint. |  |
| cli-ux/F4 | medium | `open` | `bin/mogging.mjs:1082` | Held. Purely a rendering gap — sibling verbs already render role: runOwners:617 `c.role`, runMailRead:688 `msg.role`. |  |
| command-blocks/F3 | medium | `open` | `src/ui/features/blocks/block-tracker.ts:128` | As originally recommended: render start time and duration in the gutter title (:128) and collapsed strip (:201). |  |
| command-blocks/F4 | medium | `open` | `src/ui/features/terminal/terminal-pane.ts:970-978 (tracker at block-tracker.ts:66,104)` | The delta added the exact ingredient a fix needs: the pane now stamps replayCopyGraceUntil at spawn/reattach (:558), a ready-made replay window BlockTracker cou |  |
| command-blocks/F5 | medium | `open` | `src/ui/features/blocks/block-tracker.ts:190-196` | As originally recommended: subscribe startMarker.onDispose to prune the block; filter isDisposed in reposition/jump/find. |  |
| command-blocks/F6 | medium | `open` | `src/ui/features/terminal/terminal-pane.ts:880-882` | Only the line number moved (836->880) from unrelated edits above it. |  |
| command-blocks/F7 | medium | `open` | `src/main/smokes/blocks-smoke.ts:20-39` | As originally recommended: add tests/unit/block-tracker.test.ts (exitColor, find, ring cap, readCommand cursor-row slice) and extend the smoke to dispatch Alt+A |  |
| connection-stdio-bridge-into-agent-clis-adr-0014/F4 | medium | `open` | `src/main/mcp-endpoint.ts:164` | Code unchanged. The delta's ADR 0021 REST branch widened what rides this entry point but does gate writes. Docs tension confirmed: adr/0014:161 calls the plan t |  |
| connections-oauth/F2 | medium | `open` | `src/main/connections.ts:500` | Same severity, wider window: the device branch (453-474) adds another await before line 500, and a rival flow can now sit in the separate pendingDevice slot (56 |  |
| connections-oauth/F4 | medium | `open` | `src/main/connections.ts:254` | 'Exercised by no test' is slightly overstated — CONNLIVE does call connect/cancelConnect for real. The substantive claim (no coverage of supersede/timer/cancel |  |
| context-monitor/F1 | medium | `open` | `src/backend/features/context/monitor.ts:334` | Unchanged. Real exposure: the heuristically-locked pane still loses to takeover (208 `!owner.pinned`) and mtime migration (354) despite its relay confirming ide |  |
| context-monitor/F2 | medium | `open` | `src/backend/features/context/providers.ts:25` | Severity unchanged. Both halves re-verified independently; the file has not moved and the claude-sink fix it should mirror is one file away in the same director |  |
| context-monitor/F3 | medium | `open` | `src/backend/features/context/monitor.ts:430` | Unchanged. The contract it breaks is still stated in the renderer: src/ui/features/context/index.ts:73-75 sets 'pending' — "never a made-up 0%" — yet an opencod |  |
| context-monitor/F4 | medium | `open` | `src/backend/features/context/monitor.ts:412` | Severity unchanged (docs/05 main-thread law). One sub-claim is smaller than stated: candidates() at 352 does route these into the codex branch, but codexDayDirs |  |
| control-api/F1 | medium | `open` | `src/main/deep-link.ts:31` | Untouched by the delta. 39e4b93 (remote insert honesty) edited controller.ts but added no local-cwd validation, so the Windows-only UNC/NTLM-probe divergence st |  |
| control-api/F2 | medium | `open` | `bin/mogging.mjs:803` | Unchanged; still violates docs/06-control-api.md:22 ('2 usage'). Slightly aggravated: the verb list at lines 40-60 grew (map, recall, mail, role, claim, owners, |  |
| curated-rest-tool-bridge-adr-0020-0021/F2 | medium | `open` | `src/main/mcp-endpoint.ts:176` | Title overstates: grep shows NO shipped restTools endpoint contains ${...}; the four connectionConfig rows (gitlab/make/n8n/sentry) declare it on methods and sh |  |
| curated-rest-tool-bridge-adr-0020-0021/F3 | medium | `open` | `src/main/smokes/restmilestone-smoke.ts:249` | Partial credit: entry/token come from the real restBridgeUpstream (line 247) and the grant from real resolveWriteAllGranted (258). Untested: the svc constructio |  |
| delta/agent-setup-onboarding/35 | medium | `open` | `src/backend/platform/env-path.ts:415` | Emit `export PATH="$PATH":"dir"` (and fish_add_path -a) to match Windows, and merge the dirs already inside the fenced block with `wanted` before rewriting it s |  |
| delta/agent-setup-onboarding/36 | medium | `open` | `src/ui/features/agents/setup-panel.ts:209` | Add a Cancel control to the running state that invokes AgentChannels.setupCancel(agentId) and surfaces the 'Cancelled.' state; keep dispose() non-cancelling so |  |
| delta/agent-setup-onboarding/37 | medium | `open` | `src/backend/platform/env-path.ts:251` | Have addToProcessPath invalidate `inFlight` too, and make applyLivePathToProcess union live.entries with the CURRENT process.env.PATH at assignment time instead |  |
| delta/agent-setup-onboarding/38 | medium | `open` | `tests/unit/env-path.test.ts:3` | Add unit tests over a fixture PATH for resolveOnPath (PATHEXT order, unstatable alias), persistWindows/persistPosix against a temp HOME/rc, and a SetupService r |  |
| delta/conpty-v2-and-pin/6 | medium | `open` | `src/main/smokes/conpty-smoke.ts:259` | Require `found > 0 && found >= count - lossMax` inside census.ok, cap STREAM's v1 ceiling below STREAM_MARKS, and add a CONPTY gate row run with MOGGING_CONPTY_ |  |
| delta/conpty-v2-and-pin/7 | medium | `open` | `scripts/build-node-helper.mjs:92` | Wrap the rmSync in try/catch as the comment intends, and sweep pre-existing `*.stale-*` files in the target dir on each overlay so the helper tree stops accumul |  |
| delta/conpty-v2-and-pin/8 | medium | `open` | `src/backend/platform/pty-host.ts:96` | Type a second marker family AFTER the narrow crossing and census both families' row order, bounding the residual with a gate; and state the Windows-only diverge |  |
| delta/conpty-v2-and-pin/9 | medium | `open` | `scripts/check-conpty-pin.mjs:11` | Record the upstream Windows Terminal tag + per-file sha256 in a manifest beside the vendored tree, verify staged files against THAT, add an Authenticode check o |  |
| delta/contracts-ipc-delta/100 | medium | `open` | `src/backend/features/agents/setup.ts:116` | Kill the tree on Windows (job object, or `taskkill /PID <pid> /T /F`) in SetupService.cancel and in the exec watchdog; on POSIX spawn detached and process.kill( |  |
| delta/contracts-ipc-delta/101 | medium | `open` | `src/backend/features/worktrees/index.ts:87` | Probe writability non-destructively (accessSync W_OK on the nearest existing ancestor of <repo>/.mogging), or create the root only inside createWorktree; otherw |  |
| delta/contracts-ipc-delta/102 | medium | `open` | `scripts/qa-smokes.sh:186` | Add a pure smoke driving SetupService over a fixture spawn seam (already-installed short circuit, runtime-missing remedy, the npm-prefix probe-failure skip at s |  |
| delta/contracts-ipc-delta/96 | medium | `open` | `src/contracts/integrations/first-party-clients.ts:70` | Extend check-catalog.mjs's SECRET_PREFIXES/entropy scan to src/contracts/integrations/first-party-clients.ts and fail on any FIRST_PARTY_CLIENTS key outside iss |  |
| delta/contracts-ipc-delta/97 | medium | `open` | `src/main/connections.ts:412` | Add `device: undefined` to the setState patches at connections.ts:412 and :1593 so every entry into 'connecting' and every boot sweep clears the code panel, as |  |
| delta/contracts-ipc-delta/98 | medium | `open` | `src/contracts/ipc/terminal.ipc.ts:68` | Stamp gen inside terminalClient.write from the pane registry (or expose sessionGen via pane-meta) so every write site carries it — above all the timer-deferred |  |
| delta/contracts-ipc-delta/99 | medium | `open` | `src/main/daemon-relay.ts:452` | Track the live gen per pane id in daemon-relay (SpawnResult.gen already arrives there) and skip the Object.assign when cmd.gen is present and mismatched, so a r |  |
| delta/daemon-protocol-v11/13 | medium | `open` | `src/main/daemon-relay.ts:452` | Skip the Object.assign when cmd.gen is present and differs from the relay's own `gens.get(id)`, mirroring the tombstone check one line above, so the replay spec |  |
| delta/daemon-protocol-v11/14 | medium | `open` | `src/pty-daemon/transport.ts:224` | Gate `kill` on `gen` the same way (optional, present-and-stale refused), and correct the protocol.ts v11 note: the gate is a staleness guard for cooperative sen |  |
| delta/daemon-protocol-v11/15 | medium | `open` | `src/pty-daemon/transport.ts:214` | Log a rate-limited `input/resize REFUSED: stale gen <m.gen> != <pane.gen> for pane <id>` and reply `{t:'error', reason:'stalegen', id}`; have DaemonClient journ |  |
| delta/defaults-tier/41 | medium | `open` | `src/backend/features/agent-settings/service.ts:817` | Have applyAccountDefaults suppress per-row `changed` and emit one provider-level event after reconcileRows returns, and/or debounce the renderer's changed handl |  |
| delta/defaults-tier/42 | medium | `open` | `src/ui/features/settings/agent-config.ts:574` | Add a second verb on tier-managed keys ("Stop managing & restore original") that runs release('restore') per home before dropping the compiled rows, or keep the |  |
| delta/defaults-tier/43 | medium | `open` | `src/ui/features/settings/agent-config.ts:423` | Either persist a per-provider 'cross-account consent given' flag in the KV store and show the dialog only on the first cross-account save, or correct the commen |  |
| delta/defaults-tier/44 | medium | `open` | `docs/adr/0022-shared-account-defaults.md:74` | Rewrite the ADR consequence to state that pins live in the `__pin__:` namespace and a pre-existing scoped override at the same home is an implicit pin fan-out w |  |
| delta/defaults-tier/45 | medium | `open` | `src/main/profiles.ts:217` | Call scheduleAccountDefaultsApply(provider) in the remove handler after removeAgentConfigTarget (resolve the provider before removeProfile), so surviving homes |  |
| delta/design-system-css/80 | medium | `open` | `src/ui/styles/global.css:7798` | Give --danger-weak a per-theme value in themes.ts (light: a tint of #c92e25), or drop the fill and keep only the 2px --danger left rule. Same for .agent-setup-r |  |
| delta/design-system-css/81 | medium | `open` | `src/main/smokes/wizardux-smoke.ts:150` | Add an AA arm to wizardux over ['.wizard-chip','.wizard-chip-count','.wizard-chip.is-armed','.wizard-alert-text']; extend homeux PROBES with the .agent-setup-st |  |
| delta/design-system-css/82 | medium | `open` | `src/ui/styles/global.css:3284` | Declare `--focus: var(--accent)` in the ring-width token block (global.css:96-104), or substitute var(--accent) at both sites. Add a static check that every var |  |
| delta/design-system-css/83 | medium | `open` | `src/ui/components/grid-painter.ts:367` | In render(), record whether canvas.contains(document.activeElement) and the region index before clearing, then re-focus canvas.querySelectorAll('.gp-region')[id |  |
| delta/design-system-css/84 | medium | `open` | `src/ui/styles/global.css:4710` | Keep the rtl box for the head-ellipsis but neutralize bidi: emit `‎${r.cwd}‎` at wizard/index.ts:961, or wrap the value in an inner span with `unicode-bidi: iso |  |
| delta/design-system-css/85 | medium | `open` | `src/ui/styles/global.css:10235` | Swap to var(--r-md), var(--fs-13)/var(--fs-24)/var(--fs-12), var(--track-wide). Extend check-spacing.mjs (or a sibling gate) to flag radius/font-size/letter-spa |  |
| delta/explorer-sync-laws/55 | medium | `open` | `src/main/smokes/fileact-smoke.ts:135` | Create two panes with different cwds (root and root/src, or a worktree), focus pane A, dispatch the drop on pane B's body, and assert the write is relative to B |  |
| delta/explorer-sync-laws/56 | medium | `open` | `src/ui/features/explorer/index.ts:373` | In onExpandedChange (:193-197) delete ignoredByDir entries for dirs no longer in [rootPath, ...expandedDirs()] before refreshIgnored, and clear the map in refre |  |
| delta/explorer-sync-laws/57 | medium | `open` | `src/ui/features/explorer/index.ts:553` | Add a capture-phase dragover/drop handler on the browser dock's guest viewHost that cancels any drag carrying EXPLORER_DRAG_TYPE (an explorer row is for panes a |  |
| delta/gate-honesty-delta/91 | medium | `open` | `scripts/check-package-weight.mjs:40` | Require freshness: have check-fuses.mjs write a dist/.packaged-at stamp and make check-package-weight.mjs refuse a stamp older than this process's start time (o |  |
| delta/gate-honesty-delta/92 | medium | `open` | `src/main/smokes/daemon-survive-smoke.ts:81` | Fix the comment to say this smoke models a MEASURED pane and RESTOREDIMS owns the unmeasured one, or add a third SURVIVE arm: spawn a second pane dims-less in p |  |
| delta/gate-honesty-delta/93 | medium | `open` | `scripts/qa-smokes.sh:357` | Raise both SURVIVE rows to 180 so the sweep's timeout stays above app boot + ensureDaemon + the 40s internal watchdog. |  |
| delta/gate-honesty-delta/94 | medium | `open` | `scripts/check-audit.mjs:22` | Add `run_static AUDIT2 node scripts/check-audit.mjs docs/research/2026-08-01-full-feature-audit.md` with a resolution/owner column the parser reads, or land a f |  |
| delta/install-engine-has-zero-executed-coverag/113 | medium | `open` | `src/backend/features/agents/setup.ts:180` | Await applyLivePathToProcess() at the top of each install-loop iteration, and add ~/Library/Python/*/bin and %APPDATA%\Python\Python*\Scripts to wellKnownBinDir |  |
| delta/install-engine-has-zero-executed-coverag/114 | medium | `open` | `src/backend/features/agents/setup.ts:458` | When persisted.ok is false, append persisted.error to the transcript and set it as the step's note plus remedy — the step may stay 'done' for this app, but must |  |
| delta/install-engine-has-zero-executed-coverag/115 | medium | `open` | `src/backend/features/agents/setup.ts:75` | Add a constructor deps object ({resolveOnPath, applyLivePath, persistPath, spawn}) defaulting to the real ones, then a pure gate (smoke:setup-pure) driving prob |  |
| delta/install-engine-has-zero-executed-coverag/116 | medium | `open` | `src/main/agents.ts:198` | Either add a Cancel control to createAgentSetupPanel's running state that invokes AgentChannels.setupCancel, or delete the channel and correct cancel()'s doc co |  |
| delta/installer-freeze-fixes/66 | medium | `open` | `electron-builder.yml:209` | Add a `!macro customUnInstall` to build/installer.nsh doing `WinShell::UninstShortcut "$oldDesktopLink"` / `Delete "$oldDesktopLink"` (and $newDesktopLink), and |  |
| delta/installer-freeze-fixes/67 | medium | `open` | `electron-builder.yml:209` | Keep createDesktopShortcut at its default (or 'always') and suppress the automatic creation another way, or add an explicit `${if} ${Silent}` CreateShortCut in |  |
| delta/installer-freeze-fixes/68 | medium | `open` | `.github/workflows/release.yml:229` | Add a `Package weight gate` step to release.yml right after the fuse wall gate, reusing the same APP resolution: `MOGGING_WEIGHT_APP="$APP" node scripts/check-p |  |
| delta/installer-freeze-fixes/69 | medium | `open` | `scripts/check-package-weight.mjs:92` | Enumerate app.asar entries with @electron/asar listPackage (or read the asar header) and run the same FORBIDDEN predicates plus a file count over those paths; s |  |
| delta/installer-freeze-fixes/70 | medium | `open` | `build/installer.nsh:117` | Add a static gate that parses EXE from build-node-helper.mjs and fails if build/installer.nsh lacks that literal in both the nsProcess probe and the Win32_Proce |  |
| delta/installer-freeze-fixes/71 | medium | `open` | `build/installer.nsh:198` | Drop the per-user sentence from MUI_WELCOMEPAGE_TEXT (or restate it in customPageAfterChangeDir where $installMode is known) and change the closing line to "Cli |  |
| delta/oauth-device-flow/49 | medium | `open` | `src/backend/features/integrations/oauth.ts:526` | Add `resource?: string` to requestDeviceCode's options and set it on the device-authorization body; pass `resource` from beginDeviceFlow (connections.ts:549-554 |  |
| delta/oauth-device-flow/50 | medium | `open` | `src/contracts/integrations/first-party-clients.ts:70` | Extend check-catalog.mjs to run SECRET_PREFIXES/entropyish over src/contracts/integrations/first-party-clients.ts, plus a mutation-red proving a `ghp_…`/high-en |  |
| delta/oauth-device-flow/51 | medium | `open` | `src/backend/features/integrations/client-registry.ts:89` | When a first-party client fails with redirect drift, set `needsClientId: true` alongside the honest sentence, restoring the paste-form escape hatch that existed |  |
| delta/persistuserpathentries-can-wipe-hkcu-pat/121 | medium | `open` | `src/backend/platform/env-path.ts:440` | In persistPosix compute the same coverage check against the resolved login-shell PATH, return only genuinely new dirs in `added`, and return `added: []` when th |  |
| delta/persistuserpathentries-can-wipe-hkcu-pat/122 | medium | `open` | `src/backend/platform/env-path.ts:268` | Have addToProcessPath invalidate `inFlight` too — bump a generation counter the in-flight IIFE checks before assigning `cached`, and re-read — so a refresh begu |  |
| delta/persistuserpathentries-can-wipe-hkcu-pat/123 | medium | `fixed` | `tests/unit/env-path.test.ts:3` | Extract the persistWindows decision as a pure exported fn (existingRaw, existingKind, machineRaw, wanted) -> {write|null, kind, added} and pin that a failed rea |  |
| delta/remote-insert-honesty/62 | medium | `open` | `src/pty-daemon/session.ts:330` | Move READY_OSC_PRINTF out of the outer bootstrap into `interactive`, immediately after the requested-cwd cd succeeds (mirroring the fish branch), so a cwd that |  |
| delta/remote-insert-honesty/63 | medium | `open` | `src/main/smokes/remote-smoke.ts:482` | On win32, drive the bite through the focus fallback the commit names: give the remote entry no cwd so setFocusedPane falls back to meta.cwd (controller.ts:298, |  |
| delta/remote-insert-honesty/64 | medium | `open` | `docs/16-files.md:237` | Rewrite the Drag paragraph: the marker carries the raw absolute path, the drop target computes the insert (remote-first), text/plain is the absolute quoted path |  |
| delta/terminal-audit-fixes/26 | medium | `open` | `src/ui/features/terminal/pane-anchor.ts:184` | Keep the deferral but add a bounded fallback: if the mode is still set N ms after stick() (or on the pane's process exit), scroll to bottom anyway; clear the we |  |
| delta/terminal-audit-fixes/27 | medium | `open` | `src/pty-daemon/session.ts:480` | Only trim when the persisted tail was actually cut: `restore.scrollback.length >= PERSISTED_SCROLLBACK_CHARS ? trimTornStart(sb) : sb`, and pin the untorn-passt |  |
| delta/terminal-audit-fixes/28 | medium | `open` | `src/main/daemon-relay.ts:131` | Assign daemonPid (or better, endpoint.token, which is regenerated per daemon and immune to pid reuse) only after `await c.connect()` resolves in makeClient, and |  |
| delta/terminal-audit-fixes/29 | medium | `open` | `src/main/daemon-client.ts:522` | Store the replay disposition per pane id in a separate map set at send time (cleared on the reply/error), so a spawn that times out or races a renderer-issued s |  |
| delta/terminal-audit-fixes/30 | medium | `open` | `src/main/smokes/restoredims-smoke.ts:162` | Feed the captured replay into a headless xterm in the gate (or a unit test) and assert buffer.active.cursorY lands on the last populated row and that the shell' |  |
| delta/terminal-dims-restore/19 | medium | `open` | `src/ui/features/terminal/terminal-pane.ts:510` | Read once: `const g = proposeGrid(this.term)` and send `cols: g?.cols, rows: g?.rows` (or applyGrid(term, g) before spawning), so the dims asserted as measured |  |
| delta/terminal-dims-restore/20 | medium | `open` | `src/pty-daemon/session.ts:757` | Gate the grace on subscribers: on expiry flush only when this.subs.size === 0, otherwise re-arm. A connected client that has not measured yet is a pane the app |  |
| delta/terminal-dims-restore/21 | medium | `open` | `src/backend/features/workspace/session-rows.ts:46` | Clamp at both ends in one place — add a ceiling (e.g. 2000 cols / 1000 rows) to gridFor, asGridDim and specDimsUsable/attachDims — so an implausible measurement |  |
| delta/two-adrs-both-numbered-0022/126 | medium | `open` | `src/main/profiles.ts:219` | Call scheduleAccountDefaultsApply(profile.provider) in ProfileChannels.remove right after store.removeAgentConfigTarget('profile', id), and add a profiledefault |  |
| delta/two-adrs-both-numbered-0022/127 | medium | `open` | `src/backend/features/agent-settings/service.ts:700` | Parse each home's document once per surface and read every path off the parsed value (a codec readMany or a parse cache keyed by file text), and limit the scan |  |
| delta/two-adrs-both-numbered-0022/128 | medium | `open` | `src/main/smokes/profiledefaults-smoke.ts:59` | Add a bite that runs fan-out through main's real resolveContext with CLAUDE_CONFIG_DIR poisoned to a profile's home, asserting providerHomes dedupes by resolved |  |
| delta/wizard-redesign/74 | medium | `open` | `src/ui/features/wizard/index.ts:1701` | In isolationHint (or syncIsolate), branch on selection.state().remote first and return a terminal line such as 'Not available on a remote host.' instead of fall |  |
| delta/wizard-redesign/75 | medium | `open` | `src/ui/features/wizard/index.ts:1996` | Extend WIZARDUX: arm a brush via __mogging.wizardAgents.arm, paint slot 2 by a real pointer sweep on .gp-canvas, launch, then assert workspace.active().assignme |  |
| delta/wizard-redesign/76 | medium | `open` | `src/ui/components/input.ts:65` | Return a dispose() from createPathInput that calls retail.disconnect(), and call it from the wizard's render()/leave() beside selection?.dispose() and cdLine?.d |  |
| delta/wizard-redesign/77 | medium | `open` | `src/ui/features/wizard/index.ts:1264` | Pass the anchor tile as returnFocus from grid-painter's onPickSlot call (and from openSlotPicker), and after renderAgentControls() re-focus `.gp-region` at the |  |
| delta/worktree-preflight-split-git-timeouts/105 | medium | `open` | `src/backend/features/worktrees/index.ts:72` | Stop parsing prose: spawn git with LC_ALL=C/LANG=C (via mergeEnv in platform/env-path) and classify from exit status — 128 from rev-parse --is-inside-work-tree |  |
| delta/worktree-preflight-split-git-timeouts/106 | medium | `open` | `src/ui/features/wizard/index.ts:1701` | In isolationHint, branch on selection.state().remote first and return an explicit line ('Isolation runs on this machine only'), so the permanently disabled chec |  |
| delta/worktree-preflight-split-git-timeouts/107 | medium | `open` | `src/main/wizard-audit-faults.ts:7` | Add worktreeRemoveFailAt/worktreeRemoveDelayMs to WizardAuditFaultConfig; assert a refused rollback leaves the worktree LISTED and the wizard really says 'needs |  |
| delta/worktree-preflight-split-git-timeouts/108 | medium | `open` | `src/main/worktrees.ts:36` | Make preflight genuinely non-mutating (see the mkdir finding), and in the handler reject non-absolute paths and paths that do not already resolve to an existing |  |
| deps-hygiene/F2 | medium | `open` | `package.json:14` | Unchanged. Still a Windows/macOS workflow divergence, latent only because no Electron major landed (still ^39.8.10). Couples to F5: the bump F5 asks for springs |  |
| deps-hygiene/F3 | medium | `open` | `scripts/qa-smokes.sh:218` | Unchanged severity, wider blind spot: static gates grew 30->32 and the shipped tree gained a vendored conpty.dll/OpenConsole.exe pair (build/conpty/1.25.2603030 |  |
| deps-hygiene/F4 | medium | `open` | `scripts/build-node-helper.mjs:165` | Unchanged. Worth noting :205-208 (if the builder's own node IS the pin, copy process.execPath) means machines on 24.15.0 never hit the network — narrowing, not |  |
| deps-hygiene/F5 | medium | `open` | `package.json:62` | Case is stronger: major Windows-backend work landed (ConPTY v2, vendored 1.25 pin, check-conpty-pin.mjs) without touching the Electron major. Raise to high only |  |
| docs-drift/F1 | medium | `open` | `docs/02-mvp-and-roadmap.md:17` | Untouched by the delta. Sharper now: README.md:164-166 marks Phase 0/1/2 ✅ itself, while the file it calls the 'Full plan' says Phase 0 is current. |  |
| docs-drift/F2 | medium | `open` | `docs/02-mvp-and-roadmap.md:165` | A second present-tense citation the original missed: docs/02:456 ('ADR 0009's law 1'). The delta's real ADR 0022 collision (F4) is the predicted collision alrea |  |
| docs-drift/F3 | medium | `open` | `docs/02-mvp-and-roadmap.md:315` | No diff to docs/02 since c026463, and no label-vs-filename check was added — check-docs-refs.mjs at HEAD still only existsSync's the target (:45). |  |
| docs-drift/F4 | medium | `open` | `docs/adr/0022-shared-account-defaults.md:1` | Now load-bearing, not theoretical: the accepted 0022 is cited by docs/22-shared-defaults.md:6 and ~14 code sites (settings-store.ts:45,205,285,458; agent-settin |  |
| docs-drift/F5 | medium | `open` | `scripts/check-docs-refs.mjs:30` | One more citer than originally reported (prompts/phase-12/README.md:72). Gate-honesty holds: DOCSREFS prints 'every relative link resolves' with README and all |  |
| docs-drift/F6 | medium | `open` | `docs/06-control-api.md:11` | Unchanged by the delta. The usage() banner at bin/mogging.mjs:64-77 is now the only complete enumeration, so `mogging --help` beats the doc that calls itself th |  |
| error-resilience/F1 | medium | `open` | `src/pty-daemon/index.ts:44` | Held at the prior verifier's corrected medium: data is set aside, not destroyed; the lock case self-guards (rename also fails, swallowed :46-48). Real triggers |  |
| error-resilience/F2 | medium | `open` | `src/main/native-preflight.ts:68` | Untouched by the delta despite ADR 0017 work elsewhere. The new PATH repair (boot.ts:263-266) runs before assertNativeModules and does not touch helper natives. |  |
| error-resilience/F3 | medium | `open` | `src/pty-daemon/transport.ts:175` | The neighbouring 'badremote' send at :160 proves the error-frame channel works; only the throw path misses it. The new MOGGING_DAEMON_SPAWN_DELAY_MS seam (:194- |  |
| error-resilience/F4 | medium | `open` | `src/main/boot.ts:368` | Slightly worse: the new PATH repair adds `await Promise.race([applyLivePathToProcess(), 4000ms])` at boot.ts:263-266 ahead of it. registerRuntimeHealth is still |  |
| error-resilience/F5 | medium | `open` | `src/main/fatal.ts:51` | The delta strengthened daemon survival (protocol v11, reconnect replay modes), which makes the missing relaunch path more valuable: sessions demonstrably surviv |  |
| event-bridge/F2 | medium | `open` | `src/main/event-bridge.ts:169` | File unchanged since baseline. No new gate covers it: evbridge-smoke.ts configures only pasted URLs (lines 43/55/93), never an env-ref, so a further regression |  |
| event-bridge/F3 | medium | `open` | `src/main/event-bridge.ts:60` | Unchanged since baseline. Still contradicts the file's own doctrine at :76 ('Resolve the URL for delivery ONLY'). Held at medium: custody hygiene plus syscall l |  |
| event-bridge/F4 | medium | `open` | `src/backend/features/integrations/bridge.ts:80` | bridge.ts is byte-identical to baseline; the delta added no tests here. |  |
| explorer-files/F1 | medium | `open` | `src/ui/components/file-tree.ts:653 (+trigger :701)` | Kept at the prior verifier's medium, not the original high: the ghost survives until the dir is expanded and then changes or Refresh runs (refresh() re-lists ro |  |
| explorer-files/F3 | medium | `open` | `src/ui/components/file-tree.ts:307` | Unchanged by the delta; plain wheel-scrolling after clicking a row reaches it, and an APG tree losing focus to body is an accessibility defect. |  |
| explorer-files/F4 | medium | `open` | `src/ui/features/explorer/index.ts:372` | Slightly worse post-delta: the reopen rearm at index.ts:619 is a fourth droppable caller, so a reopen during a live pass leaves the restored tree undimmed until |  |
| explorer-files/F5 | medium | `open` | `src/backend/features/explorer/watch.ts:34 (no test in tests/unit)` | More pointed post-delta: 5ce4bdc shipped a whole new state machine (stale flag, flight-slot release, rearm door) on smoke coverage alone — F1, F2 and F4 all sit |  |
| home-updates-ui/F1 | medium | `open` | `src/main/updater.ts:55` | Held at the prior verifier's medium, not the original HIGH: needs a download in flight at the exact ms of window destroy. Nothing in the 32-commit delta widened |  |
| home-updates-ui/F2 | medium | `open` | `src/ui/features/updates/index.ts:90` | The delta added boot.ts:399 claiming 'the renderer's update UX calls both the moment it mounts' — only half true: Settings pulls, the rail/dot/toast feature doe |  |
| home-updates-ui/F3 | medium | `open` | `src/ui/features/updates/index.ts:111` | Untouched by the delta; planned-but-unlanded work, not a regression. Still a real honesty cost: the user is asked to restart with zero information about what th |  |
| ipc-preload-contracts/F1 | medium | `fixed` | `src/main/libfetch.ts:29` | Held at the prior verifier's corrected medium: needs local env control plus per-workspace distill consent, default OFF (libfetch.ts:6-8). Nothing in the 32-comm |  |
| ipc-preload-contracts/F2 | medium | `open` | `src/main/electron-context.ts:14` | Unchanged. The delta added MORE direct registrations (agent-global-hooks.ts:217-227, one-click setup agents.ts:192-197), widening the unvalidated surface; compe |  |
| ipc-preload-contracts/F3 | medium | `open` | `scripts/check-originpin.mjs:100` | Same defect, broader than scoped: usage/status adds more provider endpoints outside the table. Those ride the user's own credential, so a fix should separate ho |  |
| ipc-preload-contracts/F4 | medium | `open` | `src/preload/index.ts:17` | Unchanged, and a gate-honesty exposure: the sweep gates the LIST, never the ENFORCEMENT, so deleting assertAllowed from `send` leaves every gate green. |  |
| ipc-preload-contracts/F5 | medium | `open` | `src/contracts/ipc/channels.ts:494` | The delta widened scope, not kind: six new brain channels (drafts/draftGet/draftPromote/draftDiscard/distillGet/distillSet) landed with payload shapes documente |  |
| ipc-preload-contracts/F6 | medium | `open` | `src/preload/index.ts:14` | terminal-pane.ts gained more per-pane global subscriptions filtered identically (:1030,:1130,:1156,:1472,:1707,:1751,:1754), so the same O(panes) crossing now s |  |
| local-endpoint-token-custody-and-lifecycle/F3 | medium | `open` | `src/main/mcp-endpoint.ts:596` | Unchanged. Bin clients still cannot distinguish a crash-stale file from a live one; the 'app is not running' misreport path is intact. |  |
| local-endpoint-token-custody-and-lifecycle/F4 | medium | `open` | `src/backend/platform/runtime-paths.ts:42` | Unchanged, and a live platform-parity violation: on Windows the trust-root token rests solely on inherited %LOCALAPPDATA% ACLs while macOS gets a real 0700 dir. |  |
| local-endpoint-token-custody-and-lifecycle/F5 | medium | `open` | `src/main/mcp-endpoint.ts:603` | Unchanged. Still bounded by stop riding before-quit, but slightly more load-bearing now: what a surviving socket keeps is the ungated OAuth proxy path (F2), whi |  |
| local-endpoint-token-custody-and-lifecycle/F7 | medium | `open` | `src/main/smokes/mcp-smoke.ts:244` | More conspicuous now: the delta added 25 test files / +2528 lines under tests/ while the trust-root handshake still has none. Gate honesty: deleting the `msg.to |  |
| mcp-registration/F2 | medium | `open` | `src/main/tool-plan.ts:108` | Unchanged. Rollback still takes no backup of its own, so the destroyed external edit is unrecoverable, unlike mcp-manager's ensureBackup path. |  |
| mcp-registration/F3 | medium | `open` | `src/main/mcp-manager.ts:74` | Medium holds, with a stronger trigger: the app itself sets this env for profile launches (agents/global-hooks.ts:32, agents/session-pool.ts:13), so apply report |  |
| mcp-registration/F4 | medium | `open` | `src/main/mcp-manager.ts:298` | Unchanged. tool-plan.ts:136-145 writes the same files through the coordinator, so two uncoordinated CAS paths on config.toml / settings.json coexist in one proc |  |
| mogging-mcp-server/F2 | medium | `open` | `bin/mogging-mcp.mjs:773` | Unchanged. handleWriteCall (:686-711) does a live per-call grant re-check so a blind direct call still works; damage is a tools/list that under-reports for the |  |
| mogging-mcp-server/F3 | medium | `open` | `docs/14-integrations.md:193` | Second stale sentence moved (baseline :280 -> :343, pushed by the OAuth insert); cited :193 is byte-identical. grant-store.ts:135-137 already states the truth i |  |
| notify-hooks/F2 | medium | `open` | `src/main/notify-hook.ts:119-120 and :131` | As originally recommended: per-launch filename (pane id or nonce suffix) plus stale-file GC on app start; the env var already carries the exact path. |  |
| notify-hooks/F3 | medium | `open` | `src/main/notify-hook.ts:130` | As originally recommended: extract opencodeDir()'s XDG-aware resolution (agent-global-hooks.ts:103-104) into a shared helper and use it for the userTui read at |  |
| notify-hooks/F4 | medium | `open` | `hooks/codex/config.toml:23` | As originally recommended: change the snippet (and README Codex row) to `notify = ["node", "<abs>/bin/mogging.mjs", "notify"]` on Windows or add an explicit .cm |  |
| palette-commands/F1 | medium | `open` | `src/ui/features/palette/index.ts:216-219, 179-190` | Palette z-index 150 > modal 100, so it opens visibly above the dialog — not an invisible keystroke, hence medium. |  |
| palette-commands/F2 | medium | `open` | `src/ui/core/a11y/overlay-trap.ts:19 and :72-74` | No overlay-trap test exists in tests/unit (34 files, none matching). |  |
| palette-commands/F3 | medium | `open` | `src/ui/features/palette/index.ts:216` | Explorer chord moved 767 -> 775; still e.key-based. |  |
| palette-commands/F4 | medium | `open` | `src/ui/features/palette/index.ts:31-34, 123` | Pane/Layout verbs at workspace/index.ts:806-851 still sort below 'Open integrations' on the empty query. |  |
| palette-commands/F7 | medium | `open` | `tests/unit (34 files); src/ui/core/commands/command-port.ts:50-64` | Add tests/unit/command-port.test.ts (runCommand hit/miss/refusal, setCommands republish) and export score() for golden ordering tests incl. the empty-query rank |  |
| panes-layout/F1 | medium | `open` | `src/ui/features/workspace/controller.ts:1161` | The scrub loop at 1152-1159 already iterates peekTemplate(count) — the correct sparse slot set is computed two lines above the dense call and simply not passed. |  |
| panes-layout/F2 | medium | `open` | `src/ui/features/workspace/controller.ts:1176` | As originally recommended: clamp n to effectiveMaxPanes(view) in requestApplyTemplate (and requestReorganize's count) and call refusePaneCap when exceeded, befo |  |
| panes-layout/F3 | medium | `open` | `src/ui/features/workspace/controller.ts:1160` | As originally recommended: have the apply callback report success and only set meta.paneCount/seed cwds on success (or re-sync meta.paneCount = a.layout.paneCou |  |
| panes-layout/F4 | medium | `open` | `src/ui/features/layout/layout-tree.ts:228` | The delta added ~700 lines of new unit tests (pane-insert, pane-shared, etc.) but none touch layout-tree mutators; gap unchanged. |  |
| per-pane-git-branch-dirty-chip-pipeline/F1 | medium | `open` | `src/backend/features/git/probe.ts:20` | Severity unchanged. Correction: the smokes cited as the model (git-smoke.ts:29, git-feature-pure-smoke.ts:14) spread ...process.env and only ADD GIT_TERMINAL_PR |  |
| per-pane-git-branch-dirty-chip-pipeline/F2 | medium | `open` | `src/backend/features/git/monitor.ts:288` | Severity unchanged. Second trigger the original missed: probeRepo memoizes per root in the tick cache (:282) and panes refresh before subscribers (:337 vs :343) |  |
| per-pane-git-branch-dirty-chip-pipeline/F3 | medium | `open` | `src/backend/features/git/monitor.ts:280` | Unchanged. Worse than reported: probe.ts:202 readManagedBase(root) runs BEFORE the divergence-cache check at probe.ts:205, and it calls readGitLayout again (rep |  |
| perf-and-perception-budgets-vs-shipped-smokes/F3 | medium | `fixed` | `src/main/smokes/perception-smoke.ts:165` | The selector currently resolves: titlebar.ts:79-84 builds IconButton({label:'Board'}), dom.ts:45 stamps aria-label, appended to .titlebar-right at titlebar.ts:1 |  |
| perf-and-perception-budgets-vs-shipped-smokes/F4 | medium | `open` | `docs/07-perception-budget.md:29` | None of the five smokes added since baseline (defaultsmilestone, defaultstore, defaultsux, profiledefaults, restoredims) times startup, yet one-click agent setu |  |
| perf-and-perception-budgets-vs-shipped-smokes/F5 | medium | `fixed` | `docs/05-perf-budget.md:48` | Path correction: the file is now src/ui/features/terminal/pane-webgl.ts. It changed +21/-5 but only on the context-loss path (release(notifyRendererChanged)); w |  |
| persistence-restore/F2 | medium | `open` | `src/pty-daemon/session.ts:1285-1288` | Code shifted from :1170 to :1285 (dims/restore work above it). The comment now rationalizes the drop, but the trigger is a transient spawn failure, so the loss |  |
| persistence-restore/F3 | medium | `open` | `src/pty-daemon/index.ts:42-49` | As originally recommended: rename or delete dbPath+'-wal' and dbPath+'-shm' alongside the main file before the fresh open. |  |
| persistence-restore/F6 | medium | `open` | `src/backend/features/workspace/session-store.ts:21-35` | The grid-dims columns added since baseline went through every hand-written list with no unit coverage; the predicted failure mode is now demonstrated live risk, |  |
| profiles-failover/F3 | medium | `fixed` | `src/main/profiles.ts:118` | git diff of profiles.ts shows only scheduleAccountDefaultsApply additions. ADR 0022 now fans a saved account's defaults out on every save (profiles.ts:204), so |  |
| profiles-failover/F4 | medium | `open` | `src/backend/features/agents/notify-hook.ts:76` | Unchanged. With F1 and F2 the automatic-failover promise (docs/09-swarm.md:86, hooks/README.md:59, index.ts:43) has no live trigger and an unreliable relaunch. |  |
| profiles-failover/F5 | medium | `open` | `src/backend/features/usage/thresholds.ts:106` | The file's own line 157 proves lapsed-resetsAt windows reach this code, so worstPct can be pinned high by a window that already reset — suppressing the suggesti |  |
| profiles-failover/F7 | medium | `open` | `tests/unit (no profiles.test.ts, no thresholds.test.ts)` | The new tests/unit/account-defaults.test.ts imports agent-settings/account-defaults — a different module. Coverage still sits behind MOGGING_PROFILES/USAGE, so |  |
| pty-daemon/F2 | medium | `open` | `src/pty-daemon/transport.ts:85` | Fan-out anchor drifted 682->709. Prior verifier's medium stands: client heartbeat (daemon-client.ts:471-490) and close-handler cleanup (transport.ts:462-469) bo |  |
| pty-daemon/F3 | medium | `open` | `src/pty-daemon/lifecycle.ts:40` | Prior verifier's downgrade stands (needs SIGKILL plus pid reuse; in-proc fallback self-heals). Still a Win/macOS parity violation under the product laws. |  |
| pty-daemon/F5 | medium | `open` | `src/contracts/daemon/protocol.ts:505` | Line drifted 493->505 (v11 comment block above it). Behavior unchanged. |  |
| pty-daemon/F6 | medium | `open` | `src/main/daemon-client.ts:445` | Line drifted 429->445 (SpawnReplayMode additions). Impact intact: index.ts:111 idle-reaps only at zero clients AND zero panes; inflated otherClients still disar |  |
| pty-daemon/F7 | medium | `open` | `src/main/daemon-sweep.ts:64` | As originally recommended: also read daemon.lock; keep the dir when that pid is alive or lock mtime is within the 30s startup grace; only 'no endpoint AND no li |  |
| qa-gates-integrity/F2 | medium | `open` | `scripts/check-gates.mjs:47-48 · src/main/index.dev.ts:510,524` | No new script does the reverse check; scripts/ list confirms only check-gates.mjs and check-prod-artifact.mjs read index.dev.ts. |  |
| qa-gates-integrity/F3 | medium | `open` | `.github/workflows/ci.yml:7,238,334,418` | Gate count grew since the audit; the mismatch widened rather than closed. |  |
| qa-gates-integrity/F4 | medium | `open` | `src/main/smokes/smoke-shell.ts:189,221-222 · scripts/qa-smokes.sh:10` | softEchoMs's own docstring (212-220) explains the Windows VM rationale but the code applies it everywhere. |  |
| qa-gates-integrity/F5 | medium | `open` | `src/main/smokes/prodmilestone-smoke.ts:399,425,429` | budgetsHold feeds the final pass at line 473 and the result JSON at 509. |  |
| remotes-ssh/F3 | medium | `open` | `src/main/daemon-relay.ts:386` | Kept at the prior verifier's corrected medium: a dead-end option in Settings plus a hidden wizard entry, not data loss. No delta work touched remotes.ts or prof |  |
| remotes-ssh/F4 | medium | `open` | `src/main/smokes/remote-smoke.ts:88-90, 278-283` | HEAD's ctxStep arms (remote-smoke.ts:395-430) do push private OSC 633 through the real ConPTY pty on Windows — partial counter-evidence — but that is the daemon |  |
| remotes-ssh/F5 | medium | `open` | `src/main/remotes.ts:79-85; src/pty-daemon/session.ts:1185` | Aggravated by the delta: after the forced respawn the resume is typed by the daemon's deferred launch (deferLaunch/LAUNCH_DIMS_GRACE_MS) into a FRESH ssh, so a |  |
| review-merge-gate/F2 | medium | `open` | `src/backend/features/review/index.ts:156` | File unchanged since baseline. UI still labels it 'binary, mode-only, or other non-rendered changes' (ui/features/review/index.ts:150), pushing users to raw git |  |
| review-merge-gate/F3 | medium | `open` | `src/backend/features/review/index.ts:205` | File unchanged since baseline; the ancestor check at :207 predates the audit and does not close this race. |  |
| review-merge-gate/F4 | medium | `open` | `src/ui/features/review/index.ts:201` | File unchanged since baseline. |  |
| security-hardening/F2 | medium | `open` | `src/main/connections.ts:518` | The :585 half stays invalid, better cited: device endpoint is catalog-pinned at provider-catalog-data.ts:98-99 (`url.startsWith('https://')`), sole consumer con |  |
| security-hardening/F3 | medium | `open` | `scripts/check-fuses.mjs:228` | Original evidence misquoted slightly: :225-226 do carry a `timedOut` term (predates baseline), so a timeout-killed boot no longer reads as death. The missing ba |  |
| security-hardening/F4 | medium | `open` | `src/main/window.ts:64` | No change in the delta; hosted agent CLIs write local HTML constantly and the dev branch is still prefix-matched (localhost:5173.evil.tld). Severity unchanged. |  |
| security-hardening/F5 | medium | `open` | `src/main/deep-link.ts:32` | No change in the delta; `ls tests/unit | grep -c deep-link` = 0. A UNC cwd spawns shells against an SMB path on Windows only — platform divergence atop the dest |  |
| security-hardening/F6 | medium | `open` | `src/main/runtime-isolation.ts:59` | No change in the delta. Platform divergence: XDG_RUNTIME_DIR="$HOME/Library/Application Support" still passes on macOS, lands on the real run/v<N> tree and reti |  |
| security-hardening/F7 | medium | `open` | `electron-builder.yml:147` | No change in the delta. Compounds F4 — production loads from file:// (window.ts:212) with a permissive nav guard, while the gate header claims 'EXACTLY the fuse |  |
| service-adapters-github/F2 | medium | `open` | `src/backend/features/integrations/services/engine.ts:114` | Related duplicate: linkCardDirect/setLink call setLinks then refresh (services.ts:87-88, 108-109) and setLinks→addLink already ticks, so every new link fires tw |  |
| service-adapters-github/F3 | medium | `open` | `src/backend/features/integrations/services/engine.ts:142` | Evidence correction: applyCardPatch returns `{ ok:false, reason:'invalid' }` on a missing store (board.ts:232-233); the auditor cited ensureBoardForKey's throw |  |
| service-adapters-github/F4 | medium | `open` | `src/backend/features/integrations/services/github.ts:58` | Import burst is worse than scored: linkCardDirect ticks twice per card (setLinks+refresh, services.ts:87-88) × 2 spawns = ~4N, so a 50-issue import can burst ~2 |  |
| service-adapters-github/F5 | medium | `open` | `src/main/services.ts:113` | Unchanged since baseline. The asymmetry is explicit in code: github-board.ts:215 knows an archived card is inert, yet the engine feeding it is never told. |  |
| service-adapters-github/F6 | medium | `open` | `src/main/services.ts:123` | The gate exists for board-minted links only — github-board.ts:53 `linkService = () => boardGhWorld()?.linkService ?? 'github'`, armed only by boardgh-smoke.ts:5 |  |
| settings-ui/F1 | medium | `open` | `src/ui/components/collapsible-card.ts:129` | Prior verifier's medium (UX-only) severity stands; the chip always renders, only the fold is forced. |  |
| settings-ui/F2 | medium | `open` | `src/ui/features/settings/index.ts:884-985` | The walk at l.897-918 depends on 6 class names; a rename breaks search silently. |  |
| settings-ui/F3 | medium | `open` | `src/ui/features/settings/index.ts:933` | No 'cc:open' listener exists anywhere in the repo; the two machine-open paths still disagree. |  |
| shared-account-defaults-tier-adr-0022/F1 | medium | `open` | `src/main/agent-settings.ts:381` | Severity unchanged. Renderer-origin IPC only, so defense-in-depth rather than high; the catalog census shows the uncertified TOML/YAML blast radius is real, not |  |
| shared-account-defaults-tier-adr-0022/F2 | medium | `open` | `src/backend/features/agent-settings/service.ts:519` | Unchanged. applyTimers appears only at service.ts:164/173/174/639/642/652 — no in-flight promise chain was added in the delta. |  |
| shared-account-defaults-tier-adr-0022/F3 | medium | `open` | `src/backend/features/agent-settings/service.ts:644` | Unchanged. The adoption smoke (profiledefaults-smoke.ts:185-189) exercises only the happy path, so no gate would catch a silent non-adoption. |  |
| shared-account-defaults-tier-adr-0022/F4 | medium | `open` | `src/ui/features/settings/agent-config.ts:475` | Unchanged — honest-labels doctrine (docs/22:78-80): a visible control offering 'Apply once' that always produces a forever-enforced row, with zero observable ef |  |
| shortcuts/F1 | medium | `open` | `src/ui/features/palette/index.ts:216 (+explorer:775, app-shell:127, settings:1058)` | Prior verifier said low-medium; delta changes nothing. Kept at band top for the one real trap: Ctrl+K over a .modal-overlay opens the palette atop a blocking di |  |
| shortcuts/F2 | medium | `open` | `src/ui/features/workspace/index.ts:708 (and palette/index.ts:216)` | The HIGH rested on platform divergence that is still wrong: isModKey (shortcuts.ts:15) is ctrlKey||metaKey everywhere, so it is stolen on macOS too. Real hosted |  |
| shortcuts/F3 | medium | `open` | `src/ui/features/workspace/index.ts:690 and :726` | Unchanged. browser/index.ts:1105-1107 already models the right idiom (`e.code === 'Equal' || e.key === '+' || e.key === '='`) — copy it rather than invent a sec |  |
| shortcuts/F4 | medium | `open` | `src/ui/core/commands/shortcuts.ts:29-83` | Slightly worse in kind: terminal-pane.ts:860-866 now carries an extended rationale for Shift+End ('RE-ARM the follow ... one behavior with two doors'), so a loa |  |
| shortcuts/F6 | medium | `open` | `src/main/smokes/kbglobal-smoke.ts:46-57` | Now provably load-bearing: F1/F2/F3/F5 all survived a 133-file delta untouched, and the three chords no gate presses are exactly F1's three unguarded listeners |  |
| swarm/F1 | medium | `open` | `src/contracts/daemon/protocol.ts:189` | Untouched by the delta. The function's own doc (177-179) promises 'partial wildcards ... all count as overlap', which ?/[ falsify — code contradicts its stated |  |
| swarm/F2 | medium | `open` | `bin/mogging.mjs:476` | Unchanged. Fails closed for the merge, but the reviewer is told 'approved' with exit 0 — a green signal over a gate that did not actually run (gate honesty). |  |
| swarm/F3 | medium | `open` | `src/pty-daemon/transport.ts:319` | Unchanged. Not a merge bypass — the real gate is appRoles in src/main/daemon-relay.ts — but a rogue pane can demote the reviewer (approve then exits 6) or wipe |  |
| swarm/F4 | medium | `open` | `src/pty-daemon/ledger.ts:26` | Substance unchanged; only the cited migrate file was rewritten into the stamp-retire flow. Retires are now rarer (otherClients guard, daemon-client.ts:265-289), |  |
| telemetry/F4 | medium | `open` | `src/main/telemetry.ts:95` | Unchanged. Bounded to already-consented-era events, at most one buffered batch, one POST — but it is still a false kill-switch claim on a privacy-facing toggle. |  |
| telemetry/F5 | medium | `open` | `src/main/telemetry.ts:52` | Held at medium, but the delta sharpens it: 32 commits landed explicit gate-honesty fixes elsewhere, yet the 'fully offline forever' invariant still has no gate. |  |
| templates/F2 | medium | `open` | `src/ui/features/wizard/index.ts:1599-1616 (savePreset)` | The launch path (index.ts:525) DOES push the shell remainder; only preset-save omits it, confirming the inconsistency. |  |
| templates/F3 | medium | `open` | `src/main/templates.ts:30; src/backend/features/workspace/settings-store.ts:893-895` | src/main/templates.ts has zero diff since baseline c026463. |  |
| templates/F4 | medium | `open` | `src/ui/core/workspace/open-service.ts:49-52; src/ui/features/workspace/controller.ts:1848` | board/launch.ts:86 still calls the seam with paneCount 1 per card launch, unguarded. |  |
| terminal-rendering/F2 | medium | `open` | `docs/01-architecture.md:13` | As originally recommended: load SearchAddon per pane with a find bar in TerminalPane.handleKey, or drop the dependency and move addon-search to docs/01's 'later |  |
| terminal-rendering/F3 | medium | `open` | `src/ui/core/clipboard/clipboard-port.ts:227` | Win/mac divergence stands: cmd/PS5.1 panes run each pasted line instantly while zsh/agent-CLI panes hold it inert — against the parity promise. |  |
| terminal-rendering/F6 | medium | `open` | `src/main/smokes/panescroll-smoke.ts:373` | This gap is why F1 still passes the PANESCROLL gate; under the gate-honesty law the smoke certifies an invariant it never exercises with real key sequences. |  |
| theme-design-system/F1 | medium | `open` | `docs/11-design-system.md:845; src/ui/styles/global.css:659,7240-7247` | Severity medium per prior verifier; docs claim unchanged and still false. |  |
| theme-design-system/F2 | medium | `open` | `src/ui/styles/global.css:10909 (also :9858)` | Slightly worse than reported: a second phantom var now exists at global.css:9858. |  |
| theme-design-system/F3 | medium | `open` | `scripts/check-spacing.mjs:19 and :41-43` | Both holes byte-for-byte unchanged from the audit baseline. |  |
| theme-design-system/F4 | medium | `open` | `src/main/smokes/aa-probe.ts:111-128` | No guard added; function body unchanged apart from surrounding context. |  |
| updater-distribution/F1 | medium | `open` | `electron-builder.yml:156 / src/main/updater.ts:21` | Held at the verifier's corrected medium. docs/10 WAS edited in the delta (gate count, v0.17.0 bumps) but the signature sentence at :87-88 was left untouched — f |  |
| updater-distribution/F3 | medium | `open` | `src/main/updater.ts:387` | Unchanged. Stakes slightly higher: the hard-kill fallback this race drops into is installer.nsh's retire, which was rewritten in the delta and found broken live |  |
| updater-distribution/F4 | medium | `open` | `.github/workflows/release.yml:50` | Unchanged. The guard still contradicts the publish-after-assets law spelled out in its own comment block at :42-48, which cites the v0.16.0 outage. |  |
| updater-distribution/F6 | medium | `open` | `src/main/updater.ts:293 (handler), :301 / :330 (untested guards)` | Code unchanged; the delta corroborates it — the fallback these paths protect shipped broken and was caught only by hand-observation (2026-07-31, 2026-08-01). Ga |  |
| usage-metering/F2 | medium | `open` | `src/ui/features/usage/index.ts:128` | Merged mode is partly insulated: severityRank reads p.pace, which toView derives from the worst window (usage.ts:137-157), so windows[0] only breaks ties there. |  |
| usage-metering/F3 | medium | `open` | `src/main/usage.ts:549` | Unchanged. The correct rule now sits 130 lines above in the same file (usage.ts:420), making 549 an isolated one-line inconsistency. |  |
| usage-metering/F4 | medium | `open` | `src/main/usage-prices.ts:69` | Unchanged; FETCH_TIMEOUT_MS still 8000 (usage-prices.ts:37), so each repeat costs up to 8s on a blocked machine. |  |
| vault-custody/F1 | medium | `open` | `src/main/usage-keys.ts:61` | Same class also at brain.ts:537-541 (embedKeySetEnvRef); `git show c026463:src/main/brain.ts` proves it predates the delta, so it is an unlisted third instance, |  |
| vault-custody/F2 | medium | `open` | `src/main/vault.ts:41` | Radius widened: vaultEncrypt now also backs the OAuth refresh token + DPoP PEM (account.ts:327-328), grants/clients (connections.ts:235,346) and entitlements (: |  |
| vault-custody/F5 | medium | `open` | `scripts/check-credential-wording.mjs:164` | Still a gap, not a breach: the 17 DENIED patterns over src/main+backend+bin give 6 hits, 4 comments and 2 narrowly-true reasons (credential-core.ts:152,178). He |  |
| vault-custody/F6 | medium | `open` | `src/main/vault.ts:57` | Raised LOW->MEDIUM: the vault KV helpers now also carry the OAuth refresh token and DPoP key (account.ts:327-328,385), grants/clients (connections.ts:235,346) a |  |
| window-chrome-clipboard/F2 | medium | `open` | `src/main/clipboard.ts:346` | Unchanged. restoreEntry (clipboard-port.ts:177) is a bare invoke, so with main never rejecting, the success toast always fires and history reorders while the cl |  |
| window-chrome-clipboard/F3 | medium | `open` | `src/ui/components/confirm.ts:30` | Severity holds; reach grew. ADR 0022 added a SECOND rememberKey confirm at agent-config.ts:426-431 (consentCrossAccount) — correctly non-danger, so it is proof |  |
| window-chrome-clipboard/F4 | medium | `open` | `src/main/browser-dock.ts:334` | Unchanged. Literal dead gap against 'event-driven F11 with zero dead gap' (docs/02:81): open the dock, click into the page, press F11 — nothing on Windows/Linux |  |
| window-chrome-clipboard/F5 | medium | `open` | `src/main/menu.ts:32` | Held at medium, not raised, with one caveat unsettleable from source: AppKit auto-inserts 'Enter Full Screen' only where a View menu exists, and a user could bi |  |
| window-chrome-clipboard/F6 | medium | `open` | `src/ui/core/clipboard/clipboard-port.ts:226` | Line moved 236 -> 226 only because the delta deleted quoteDroppedPaths/quoteWithFlavor earlier in the file (+3/-13); the body is untouched. That refactor with n |  |
| winget-homebrew-install-manifests/F2 | medium | `open` | `.github/workflows/ci.yml:564` | Delta gave live proof: package.json:3 is 0.17.0 and docs/10:156 says `gh release download v0.17.0`, yet installer.yaml:4 and cask:3 still pin 0.16.0 — RELEASING |  |
| winget-homebrew-install-manifests/F3 | medium | `open` | `packaging/winget/MoggingLabs.Workspace.installer.yaml:9` | Unchanged by the delta. The manifest still pins 0.16.0 while the app is 0.17.0, so the ARP-correlation gap is already one minor version wide in practice. |  |
| winget-homebrew-install-manifests/F4 | medium | `open` | `packaging/homebrew/Casks/mogginglabs-workspace.rb:11` | Unchanged by the delta. The Windows/macOS split is real platform divergence — Windows users update via the feed, mac tap users update via neither path — which h |  |
| winget-homebrew-install-manifests/F5 | medium | `open` | `docs/RELEASING.md:13` | Delta sharpens it: docs/10:62 was edited only to move the x64 goalpost ('deferred as of v0.17.0', was v0.16.0) while RELEASING.md:3 still promises mac-x64 — the |  |
| wizard-first-run/F2 | medium | `open` | `src/ui/features/home/firstrun.ts:209` | Unchanged severity; gate-honesty angle now explicit: firstrun-smoke.ts:62-64 creates a workspace then calls the DEV-only `__mogging.firstrun.refresh()` (home/in |  |
| wizard-first-run/F3 | medium | `open` | `src/ui/features/wizard/index.ts:1751` | Unchanged. One narrow trigger stays closed: `if (target && target === preflightCwd) return syncIsolate()` (1702) stops same-folder re-emits clearing the box — b |  |
| workspace-tabs-switching-and-per-workspace-themes/F1 | medium | `open` | `src/ui/features/workspace/controller.ts:719` | Delta never touched wireReorder. Confirmed permanent, not transient: the only tab insertions are 317, 649, 710 — nothing re-renders the rail from this.order, so |  |
| workspace-tabs-switching-and-per-workspace-themes/F2 | medium | `open` | `src/ui/features/workspace/index.ts:726` | Unchanged. Not a platform-divergence finding (breaks identically on Windows and macOS) — it violates the advertised core-navigation shortcut instead. |  |
| workspace-tabs-switching-and-per-workspace-themes/F3 | medium | `open` | `src/ui/features/workspace/controller.ts:1828` | Severity unchanged, inconsistency now wider: the delta added a renderer-safe folding rule for the explorer (src/contracts/domain/cwd.ts:16-19), so a third subsy |  |
| workspace-tabs-switching-and-per-workspace-themes/F4 | medium | `open` | `src/ui/features/workspace/index.ts:391` | Untouched by the delta. Loss stays bounded to ≤400ms of metadata, so this sits at the low end of medium — kept there because it directly erodes the restore-on-r |  |
| workspace-tabs-switching-and-per-workspace-themes/F5 | medium | `open` | `src/ui/features/workspace/model.ts:87` | Unchanged. Slightly aggravated: the delta added 8 unit test files, none touching the color model, so it is now the conspicuous untested pure module in this feat |  |
| worktrees/F1 | medium | `open` | `src/ui/features/terminal/terminal-pane.ts:1990` | Only line drift (1946 to 1990). Keeping prior verifier's corrected severity: fails safe (worktree survives), git CLI workaround exists. |  |
| worktrees/F2 | medium | `open` | `src/backend/features/worktrees/index.ts:215` | As originally recommended: large maxBuffer in git(); treat status failure as dirty in removeWorktree; mark dirty=true on status failure in listWorktrees. |  |
| worktrees/F4 | medium | `open` | `src/backend/features/worktrees/index.ts:141` | As originally recommended: on !res.ok, best-effort `git worktree remove --force`, `git branch -D mogging/<slug>`, and `git worktree prune` before returning the |  |
| worktrees/F5 | medium | `open` | `src/ui/features/terminal/terminal-pane.ts:1967` | As originally recommended: a per-project worktrees panel backed by listWorktrees with the guarded remove flow, plus cleanup offer for clean pane-less worktrees |  |
| worktrees/F6 | medium | `open` | `src/backend/features/worktrees/index.ts:137` | Re-check found a second degrading consumer beyond the probe path: review/index.ts baseFor resolves the literal 'HEAD' against the repo's live HEAD, so the revie |  |
| a11y/F5 | low | `open` | `src/ui/components/modal.ts:127` | Stays low (needs a modal whose first control opens disabled), but the trigger surface grew: the wizard redesign and one-click agent setup are step-gated UIs whe |  |
| a11y/F6 | low | `open` | `src/main/smokes/a11ymodal-smoke.ts:95` | Low as a defect but a live gate-honesty exposure: a refcount regression at overlay-trap.ts:90-96 would un-inert the shell under an open wizard with A11YMODAL gr |  |
| a11y/F7 | low | `open` | `docs/11-design-system.md:12` | Unchanged. The original fix idea is not actionable as written: check-docs-refs.mjs matches only markdown links (LINK regex, :35), has no path list, and is blind |  |
| accounts-entitlements/F5 | low | `open` | `src/main/account.ts:320` | Unchanged by the delta. Low holds - display-only misidentification in Settings, no entitlement or custody consequence. |  |
| accounts-entitlements/F6 | low | `open` | `docs/19-accounts.md:1` | Unchanged by the delta. Low holds. |  |
| agent-launcher/F1 | low | `open` | `src/backend/features/agents/install.ts:98` | Exposure shrank: providers.ts:97-101 shows all UI callers now use SetupService (which repairs PATH first); agents:install stays wired (src/main/agents.ts:192) b |  |
| agent-launcher/F7 | low | `open` | `src/backend/features/agents/logins.ts:91` | Registry moved from features/agents to core/agent-clis in the delta; the gap itself is unchanged. discoverLogins (logins.ts:118) also still iterates only the th |  |
| agent-settings-editor/F5 | low | `open` | `src/backend/features/agent-settings/service.ts:122` | No change; the ADR 0022 rewrite of service.ts did not touch mergeValues or its call site. |  |
| agent-settings-editor/F6 | low | `open` | `src/backend/core/config-files/mutation-coordinator.ts:111` | Delta added 8 unit tests, none here: account-defaults.test.ts imports only managedKeys/resolveDefault. Gap guards more now — fan-out drives many more concurrent |  |
| agent-state-attention/F3 | low | `open` | `src/backend/features/agent-state/osc-parser.ts:108-113` | as originally recommended: on OSC_INTRO in that branch start a fresh OSC (inOsc=true, discarding=false, buf=''); add a torn-OSC case to oscOverflowAsserts |  |
| agent-state-attention/F4 | low | `open` | `src/main/event-bridge.ts:209-216` | as originally recommended: export onPaneGoneForBridge(paneId) deleting the entry, call it from daemon-relay onExit beside notePaneGone (daemon-relay.ts:154), as |  |
| agent-state-attention/F5 | low | `open` | `src/backend/features/agent-state/osc-parser.ts:82` | as originally recommended: correct docs/21 §3 (and the osc-parser.ts:129 comment) to the real MAX_OSC bound, or cap non-633 OSC bodies at ~4 KB keeping the larg |  |
| board-kanban/F2 | low | `open` | `src/ui/features/board/model.ts:157-162` | A narrower defect survives: thrown failures ARE swallowed when superseded. model.ts:166-169 errors only via onError; removeCard (:219-225) is onError-only, so a |  |
| board-kanban/F5 | low | `open` | `src/ui/features/board/queue.ts:37` | Unchanged. The leak half is sharper than a listener leak: poke calls tick() unconditionally (:172-178), so a stopped engine can still pull and launch. Low only |  |
| board-kanban/F6 | low | `open` | `src/ui/features/board/launch.ts:118` | Unchanged severity, but worse than borrowed opt-ins: agents/launch.ts:81-85 sends board B's card text as a recall query anchored at workspace A, so one project' |  |
| board-kanban/F7 | low | `open` | `src/main/board.ts:77` | Unchanged. Reads as data loss against docs/18's migrate-on-first-touch promise, but rows survive on disk and a restart retries; the trigger is a one-off sqlite |  |
| brain/F4 | low | `open` | `docs/20-brain.md:136` | Severity unchanged. The delta's only docs/20 edit was the gate count at line 315 (199→207); it added gates but none over the enum's closedness, so the claim sta |  |
| brain/F5 | low | `open` | `src/backend/features/brain/index.ts:1049` | Severity unchanged. The delta altered neither drain cadence nor worker lifetime; the ConPTY v2 / multi-pane work raises the ambient load this overhead competes |  |
| brain/F6 | low | `open` | `src/backend/features/brain/store.ts:653` | Severity unchanged. Re-reading sharpens it: docs/20:77 enumerates the exception set as exactly one item, so the counters are not merely undocumented but positiv |  |
| browser-dock/F5 | low | `open` | `src/ui/features/browser/index.ts:578` | Low stands, but it is a gate-honesty instance: index.ts:1596-1597 faviconSrc/faviconCaptured read strings, never a decoded image, so browserux-smoke.ts:120/220 |  |
| ci-workflows/F7 | low | `open` | `scripts/check-package-weight.mjs:40` | Half of option (b) exists — :41-42 says 'nothing to weigh' with a pointer — but it names `npm run dist:*`/MOGGING_WEIGHT_APP, never FUSES, and still exits 1, so |  |
| cli-ux/F5 | low | `open` | `bin/mogging.mjs:453` | Held at low. Confirmed the two pane-bound verb families still disagree on the same env condition; fix is a one-line substitution. |  |
| cli-ux/F6 | low | `open` | `bin/mogging.mjs:610` | Held at low — still latent (no daemon error path for these reads). Protocol v11 gen-gating (protocol.ts:274) widens the future rejection surface slightly, not e |  |
| cli-ux/F7 | low | `open` | `bin/mogging.mjs:65` | Held. The second half of the recommendation is also unmet: tests/unit holds 34 test files and none covers bin/mogging.mjs arg parsing, so F1, F2 and F7 remain u |  |
| command-blocks/F8 | low | `open` | `src/ui/features/command-blocks/README.md:4` | As originally recommended: delete the dir or reduce the README to a one-line pointer to src/ui/features/blocks. |  |
| connection-stdio-bridge-into-agent-clis-adr-0014/F6 | low | `open` | `src/main/mcp-endpoint.ts:596` | Unchanged. The delta did not widen this file's reach - the ADR 0022 credential helper is still unimplemented at HEAD - so the original LOW holds. |  |
| connections-oauth/F3 | low | `open` | `src/main/connections.ts:1051` | Still latent (only github-mcp.json declares a device endpoint), but the delta shipped the device route, so the path is now reachable in production rather than d |  |
| connections-oauth/F5 | low | `open` | `src/main/connections.ts:1593` | More reachable now: `device` (written at 577) never existed before the device route shipped. Still low — the UI renders devicePanel only while 'connecting'. |  |
| connections-oauth/F6 | low | `open` | `scripts/device-flow-live-check.ts:42` | Severity unchanged. Dev-only manual check (npm run check:device-flow-live), not a gate, so gate-honesty is not implicated — but the comment asserts an equality |  |
| context-monitor/F5 | low | `open` | `src/backend/features/context/window.ts:61` | Unchanged (low). window.ts:48-51 itself documents the ambiguity that makes it reachable (a transcript says claude-opus-4-8 at both 200K and 1M), but it needs tw |  |
| context-monitor/F6 | low | `open` | `docs/07-perception-budget.md:1` | Unchanged. Delta that matters: docs/22 is now 22-shared-defaults.md (ADR 0022), so the proposed 22-context-gauge.md would collide. The audit landing in docs/res |  |
| control-api/F3 | low | `open` | `src/main/deep-link.ts:84` | Lowered from medium: the original evidence overstated it. control2-smoke.ts calls sanitizeControl directly (line 24) and lines 92-97 assert 5 hostile payloads s |  |
| control-api/F4 | low | `open` | `src/pty-daemon/session.ts:885` | Same defect at a new line number; nothing in the delta (protocol v11, replay-integrity work) touched captureTail. |  |
| control-api/F5 | low | `open` | `bin/mogging.mjs:1096` | Unchanged, but mildly aggravated: remote work landed (remote insert honesty; HEAD cbda0ec turns the REMOTE gate green on Windows), so non-empty REMOTE cells are |  |
| control-api/F6 | low | `open` | `src/pty-daemon/transport.ts:214` | Protocol v11 added a THIRD silent-drop path: the gen gate at transport.ts:214 discards input with no reply. runSend (mogging.mjs:1126) is safe only because it o |  |
| curated-rest-tool-bridge-adr-0020-0021/F4 | low | `open` | `src/backend/features/integrations/credential-core.ts:226` | Lowered from medium: every shipped row is {in:header, header:Authorization} (cf-bindings:44, cf-dns-analytics:43, cf-graphql:43, posthog:57, stripe:43). Zero us |  |
| curated-rest-tool-bridge-adr-0020-0021/F5 | low | `open` | `src/backend/features/integrations/rest-bridge.ts:220` | Unchanged by the delta. The one shipped instance stays defensible (CF GraphQL analytics is query-only), so the exposure is that a single data PR can repeal the |  |
| curated-rest-tool-bridge-adr-0020-0021/F6 | low | `open` | `src/backend/features/integrations/rest-bridge.ts:201` | Unchanged. Bounded as originally reasoned (one retry; MAX_PAGES-capped loop), but it is a connection hold in the long-lived main process that serves every pane' |  |
| delta/agent-setup-onboarding/39 | low | `open` | `src/backend/features/agents/setup.ts:229` | Treat ran.timedOut as a verify failure with its own remedy ('the CLI started but never answered --version'), so the step verdict and the transcript cannot contr |  |
| delta/defaults-tier/46 | low | `open` | `src/ui/features/settings/agent-config.ts:604` | Render the chip as `${suggestion.homes} of ${accountCount} accounts already use X — make this the default everywhere?`, or drop suggestions whose vote count is |  |
| delta/explorer-sync-laws/58 | low | `open` | `src/ui/components/file-tree.ts:446` | Bound it: keep children for the N most recently collapsed dirs (LRU by collapse order) and null the rest out in markUnwatched, or drop children once nodes.size |  |
| delta/explorer-sync-laws/59 | low | `open` | `docs/16-files.md:237` | Rewrite the Drag paragraph: marker = raw absolute path, text/plain = quoted absolute for outside targets, insert computed by the RECEIVING pane. Add the cache-f |  |
| delta/installer-freeze-fixes/72 | low | `open` | `build/installer.nsh:100` | Correct the comment to say the macro expands in BOTH Section "install" and un.checkAppRunning, and note that SetDetailsPrint both (installer.nsh:104) therefore |  |
| delta/oauth-device-flow/52 | low | `open` | `src/contracts/integrations/connections.ts:168` | Either render the remaining minutes (and a 'this code has lapsed' line past expiresAt) in devicePanel, or delete the countdown promise from the DeviceSignIn doc |  |
| delta/oauth-device-flow/53 | low | `open` | `docs/14-integrations.md:304` | Correct docs/14-integrations.md:304 to 44, and prefer deriving the number the way check-gate-count.mjs already derives the sweep size rather than restating it i |  |
| delta/two-adrs-both-numbered-0022/129 | low | `open` | `docs/adr/0022-connections-reach-the-terminal.md:1` | Renumber the file to 0023 (it is still Proposed), replace line-anchored citations with symbol names, and rewrite 'What the user's token actually is' against the |  |
| delta/wizard-redesign/78 | low | `open` | `src/ui/features/wizard/index.ts:1018` | Call renderAgentControls() instead of refreshAgents() in the Reset-grid handler, matching the painter's own onChange (index.ts:984-989), so the chips' ×N and th |  |
| delta/worktree-preflight-split-git-timeouts/109 | low | `open` | `src/ui/features/wizard/index.ts:1702` | Expire the cache on a cheap signal (re-ask when the wizard view regains focus/visibility), or give every refusal a 'Check again' action that clears preflightCwd |  |
| deps-hygiene/F6 | low | `open` | `package.json:41` | Unchanged. Incidental: two distinct ADRs now both carry number 0022 (0022-connections-reach-the-terminal.md, 0022-shared-account-defaults.md) — out of scope but |  |
| deps-hygiene/F7 | low | `open` | `package.json:57` | Unchanged. Scope widened slightly: ci.yml went from one to four Node-22 setup steps in the delta, so more places assume a Node floor that is still nowhere decla |  |
| docs-drift/F7 | low | `fixed` | `docs/05-perf-budget.md:10` | The suggested grep pays off: same class is stale twice more — docs/11-design-system.md:12 cites src/main/setshell-smoke.ts and docs/14-integrations.md:85 cites |  |
| docs-drift/F8 | low | `open` | `README.md:157` | Understates by 23 files now (delta added two ADRs), and will drift again on every ADR landed. |  |
| error-resilience/F6 | low | `open` | `src/main/fatal.ts:49` | Lowered from medium: the reachable pre-ready window is narrow (single-instance lock + installDeepLinkListeners, boot.ts:225-231); installCliRuntime's fatal() ru |  |
| error-resilience/F7 | low | `open` | `src/main/daemon-client.ts:313` | Unchanged. The Windows half is sharper (lingering open handles on daemon.log block rotation/deletion), a parity-law smell even though the leak itself is cross-p |  |
| error-resilience/F8 | low | `open` | `src/ui/core/async/async-state.ts:134` | The delta added surfaces that all route failures here (wizard redesign, one-click agent setup, OAuth device flow), so the drift surface grew while coverage stay |  |
| event-bridge/F5 | low | `open` | `src/main/event-bridge.ts:209` | Unchanged since baseline. The gate that looks like it covers this does not: attention-smoke.ts:569-571 calls notePaneGone(1) then feeds 'idle' BEFORE 'attention |  |
| event-bridge/F6 | low | `open` | `src/main/event-bridge.ts:242` | Unchanged since baseline. Sharper than first written up: the test payload not only adds `note`, it OMITS `card`, so a flow pinned to the test run breaks on the |  |
| explorer-files/F6 | low | `open` | `src/ui/features/explorer/index.ts:624` | The new explorerrace-smoke.ts drives this exact timing (delayed src listing, fast switch back) but asserts only rootPath/rowNames/gitRoot/handles, so it walks s |  |
| explorer-files/F7 | low | `open` | `src/ui/components/file-tree.ts:245 (docs/16-files.md:163)` | Unchanged. The chip still counts gitFiles.length (index.ts:390), so it can exceed the rows the lens can show; folder tint does propagate (rebuildDecorations :35 |  |
| home-updates-ui/F4 | low | `open` | `src/ui/features/updates/index.ts:96` | Unchanged. One correction: the auditor quoted the condition as `'checking' || 'available' || ...`; the real line repeats `s.phase ===` on each arm, so it is not |  |
| home-updates-ui/F5 | low | `open` | `src/ui/features/home/firstrun.ts:15` | The delta sharpened the drift: the new comment at firstrun.ts:132-140 argues at length that installing IS correct and ADR 0002 concerns auth — contradicting the |  |
| home-updates-ui/F6 | low | `open` | `src/ui/features/home/firstrun.ts:209` | Unchanged. `completedToasted` only de-dupes within a session; it does not encode 'the card was ever incomplete', so it does not touch this trigger path. |  |
| ipc-preload-contracts/F7 | low | `open` | `docs/01-architecture.md:18` | Unchanged, and it compounds F5: bridge.ts:5-7 is unknown-in/unknown-out, so the same sentence is wrong about both the surface NAME and the claim that the IPC is |  |
| local-endpoint-token-custody-and-lifecycle/F6 | low | `open` | `src/main/mcp-endpoint.ts:594` | Unchanged. Exposure still mitigated by the 0700 parent dir (runtime-paths.ts:44), which keeps this low; the sibling-endpoint divergence and macOS-only crash res |  |
| mcp-registration/F5 | low | `open` | `src/backend/features/integrations/writers/json-dialect.ts:27` | Unchanged Windows-leaning parity gap. Additionally isManagedScopedJson (json-dialect.ts:108-115) returns false on a BOM file, so a BOM also turns a scoped tool- |  |
| mcp-registration/F6 | low | `open` | `docs/14-integrations.md:478` | Unchanged. The docs/14 rewrite in this delta touched other sections and left both drifts, so this is live drift, not a stale citation. |  |
| mcp-registration/F7 | low | `open` | `src/backend/features/integrations/writers/codex.ts:70` | Unchanged, but sharper as a gate-honesty concern: F1 shows the toolplan smoke passing on a fixture that structurally cannot reach the broken path — exactly the |  |
| mogging-mcp-server/F1 | low | `open` | `src/main/mcp-endpoint.ts:451` | Holds at the prior verifier's LOW, not the original HIGH: `by` is server-derived after verifyPaneToken (:427-435) and trail.ts:54 drops unattributable rows, so |  |
| mogging-mcp-server/F4 | low | `open` | `bin/mogging-mcp.mjs:115` | Unchanged; fail-closed is still correct, only the diagnostic misdirects. One-click agent setup + live PATH landed this window, making hand-launched MCP processe |  |
| mogging-mcp-server/F5 | low | `open` | `src/main/mcp-status.ts:119` | Unchanged. Added angle: minimize/restore event ordering differs between Windows and macOS, so a last-writer-wins flag fed by every window is also a latent platf |  |
| mogging-mcp-server/F6 | low | `open` | `src/backend/features/integrations/grant-store.ts:139` | Unchanged in substance, sharper after the delta: F1's path is the only defense-relevant one here with zero coverage either way, and the shared-defaults tier (AD |  |
| notify-hooks/F1 | low | `open` | `src/backend/features/agents/notify-hook.ts:297` | Keeping prior verifier's downgrade to low: skip is documented-deliberate, chime+notify (done/OSC 9) still wire per-session, and the global twin (global-hooks.ts |  |
| notify-hooks/F5 | low | `open` | `src/ui/features/notify/index.ts:68` | As originally recommended: key the cooldown by (paneId, tone), or let an 'attention' transition bypass a cooldown set by a 'done' toast. |  |
| notify-hooks/F6 | low | `open` | `bin/mogging.mjs:1322-1331` | As originally recommended: hoist the paneId/endpointFile no-op guard above the stdin read in runNotify to match the generated twin's ordering. |  |
| palette-commands/F5 | low | `open` | `src/ui/shell/app-shell.ts:124-134` | Register a 'rail:toggle' command (hint 'App', kbd 'Ctrl+Shift+B') from app-shell.ts calling toggleRail(), matching the explorer pattern. |  |
| palette-commands/F6 | low | `open` | `src/ui/features/palette/index.ts:15-28` | Split the query on whitespace, require every token to match title+hint (prefix/word-start/substring, summed); keep single-token subsequence as fallback tier. |  |
| palette-commands/F8 | low | `open` | `docs/11-design-system.md:100, 411, 727` | Add a palette/commands section to docs/11 stating the registry model, availability/refusal contract, empty-query rank intent, and the one-tab-stop keyboard cont |  |
| panes-layout/F5 | low | `open` | `src/ui/features/layout/README.md:61` | As originally recommended: update the README perf section and the layout-tree.ts:541 comment to name ABS_MAX_PANES/MAX_LEAVES (32) as the cap, with ~16 as the W |  |
| per-pane-git-branch-dirty-chip-pipeline/F5 | low | `open` | `src/backend/features/git/monitor.ts:129` | Unchanged. The leak needs no subdirectory cwd: the fallback returns raw cwd without path.resolve while findRepoRoot returns the resolved form (repo.ts:15). Cost |  |
| per-pane-git-branch-dirty-chip-pipeline/F6 | low | `open` | `src/backend/features/git/probe.ts:375` | Unchanged; if anything understated. Grepping the gated pure smoke (qa-smokes.sh:253, GITPURE) for GIT_FILES_CAP/truncat/rename/conflict/unborn/detached returns |  |
| perf-and-perception-budgets-vs-shipped-smokes/F6 | low | `open` | `docs/05-perf-budget.md:10` | smoke-shell.ts is byte-identical to baseline. One point the original missed: softEchoMs (smoke-shell.ts:221, x3) also relaxes the docs/07:25 60ms echo row on so |  |
| persistence-restore/F5 | low | `open` | `src/pty-daemon/session.ts:1257` | Moved from :1143 to :1257 by the dims-restore additions. The remote-insert-honesty delta did not touch this line; the windows dialect is one seam-change away fr |  |
| persistence-restore/F7 | low | `open` | `docs/01-architecture.md:43` | As originally recommended: scope the doc line to 'planned' / drop 'command-block history', or persist block boundaries alongside the pane row if still intended. |  |
| profiles-failover/F6 | low | `open` | `src/ui/features/agents/index.ts:69` | Unchanged. The delta hardened per-slot profile persistence (manifest follows failover, profpersist smoke), sharpening the contrast: the profile survives a resta |  |
| pty-daemon/F8 | low | `open` | `src/pty-daemon/index.ts:101` | As originally recommended: unlink the socket path beside clearEndpoint() when platform !== 'win32', plus a startup pass in lifecycle.ts removing daemon-*.sock f |  |
| qa-gates-integrity/F6 | low | `open` | `src/main/harness-install.ts:65-69` | MOGGING_INPROC=1 npm run dev still lands in the empty usage world. |  |
| remotes-ssh/F6 | low | `open` | `scripts/remote-bootstrap-pure-smoke.ts:82` | Latent, not live (~10 KB bootstrap). ConPTY v2 / the vendored node-pty 1.25 pin change neither CreateProcess's 32,767-char limit nor node-pty's backslash-escapi |  |
| review-merge-gate/F5 | low | `open` | `src/ui/features/review/index.ts:197` | Both UI and contract files unchanged since baseline. |  |
| review-merge-gate/F6 | low | `open` | `src/backend/features/review/index.ts:85` | File unchanged since baseline. Failure is identical on both platforms, so no platform-divergence escalation. |  |
| security-hardening/F8 | low | `open` | `scripts/qa-smokes.sh:308` | No change in the delta; one extra stale site the original missed (qa-smokes.sh:324). check-docs-refs.mjs still passes because 18-board.md exists. adr/0016:174 s |  |
| service-adapters-github/F7 | low | `open` | `src/backend/features/integrations/services/github.ts:112` | Line shifted 113→112 (github.ts is byte-identical to baseline; the original cite was one line off). Same defect, same function; severity unchanged. |  |
| service-adapters-github/F8 | low | `open` | `src/main/services.ts:55` | Auditor missed integmilestone-smoke.ts:419-441 (f), which asserts pane attention + review-changed — but its onTransition at :424-429 is a hand-copied duplicate |  |
| settings-ui/F4 | low | `open` | `src/ui/features/settings/index.ts:950` | Usage grid rows carry no indexed class, so the sharpest repro is an integrations toggle-row hit after a render(), not a usage row. |  |
| settings-ui/F5 | low | `open` | `src/ui/features/settings/usage.ts:693-702` | The always-visible Providers header announces nothing to a screen reader when a plan is hot. |  |
| settings-ui/F6 | low | `open` | `src/ui/features/settings/index.ts:78 and :886` | l.34 is a third drift site the original finding did not list. |  |
| shared-account-defaults-tier-adr-0022/F5 | low | `open` | `src/ui/features/settings/agent-config.ts:428` | Unchanged. The message line drifted 427->428 inside the same confirmDialog block — same defect, not a move. |  |
| shared-account-defaults-tier-adr-0022/F6 | low | `open` | `src/backend/features/agent-settings/service.ts:444` | Unchanged. Needs a hand-authored CLAUDE_CONFIG_DIR=~/.claude, so low — but once hit, one row reports 'drifted' forever and every reconcile rewrites the file. |  |
| shared-account-defaults-tier-adr-0022/F7 | low | `open` | `src/main/smokes/profiledefaults-smoke.ts:131` | Unchanged, plus a gate-honesty edge: the smoke reports `primaryFullMember: true` (line 226) on the identity assert at 111 alone, while the wiring it serves — a |  |
| shortcuts/F5 | low | `open` | `src/ui/core/commands/shortcuts.ts:15` | Severity holds. shortcuts.ts:5-6 actively documents the wrong behavior as intended ('the handlers accept on every platform via ctrlKey/metaKey'), so the comment |  |
| swarm/F5 | low | `open` | `src/pty-daemon/ledger.ts:19` | Unchanged. Correction to the original fix: sessions.workspaces() returns PersistedWorkspace (workspace.ipc.ts:37-42) = id/name/layout/updatedAt only — no ordina |  |
| swarm/F6 | low | `invalid` | `src/main/smokes/swarm-smoke.ts:40` | Dropped medium->low: the auditor read the smoke in isolation, missing that the harness exports LOCALAPPDATA on all platforms. The claimed failure would also thr |  |
| swarm/F7 | low | `open` | `src/contracts/daemon/protocol.ts:177` | Unchanged, and it is the direct reason F1 survived 32 commits: a table test would have caught 'router?.ts' vs 'router1.ts' immediately, and the smokes structura |  |
| swarm/F8 | low | `open` | `src/pty-daemon/mailbox.ts:41` | Unchanged, and the delta sharpens it: v11 (protocol.ts:13-20, transport.ts:207-226) added gen gating to input/resize precisely because pane ids are reused. Dire |  |
| telemetry/F6 | low | `open` | `src/main/boot.ts:431` | Unchanged. Drops data rather than leaking it, and only for opted-in users; purely a data-quality issue for short launch-and-quit sessions. |  |
| templates/F5 | low | `open` | `src/backend/features/templates/resolve.ts:5; src/ui/features/layout/templates.ts:19` | resolve.ts is unchanged since baseline. |  |
| templates/F6 | low | `open` | `src/main/templates.ts:13; src/ui/features/wizard/index.ts:355-359` | Docs still silent: only docs/research/2026-08-01-full-feature-audit.md mentions 06b/provider-mix. PRESETS still ships 4 unused mixes. |  |
| templates/F7 | low | `open` | `tests/unit/resolve-layout.test.ts:1-43 (34 files in tests/unit)` | resolve-layout.test.ts itself has no commits since c026463. |  |
| terminal-rendering/F4 | low | `open` | `src/ui/features/terminal/terminal-pane.ts:2266` | As originally recommended: surface a 'Save transcript…' menu entry, or drop the addon load and method until the snapshot feature lands. |  |
| terminal-rendering/F5 | low | `open` | `docs/05-perf-budget.md:10` | One-line fix: point docs/05-perf-budget.md:10 at src/main/smokes/milestone-smoke.ts. |  |
| terminal-rendering/F7 | low | `open` | `src/ui/features/terminal/pane-webgl.ts:164` | As originally recommended: decay glLosses on a timer (e.g. reset after 60s without a loss) or re-arm one retry on window focus, keeping gl.context_lost telemetr |  |
| theme-design-system/F5 | low | `open` | `docs/11-design-system.md:196,215; src/ui/features/workspace/model.ts:45` | Line numbers shifted (214-215 to 196/215; 44-46 to 45); the stale text is identical. |  |
| theme-design-system/F6 | low | `open` | `src/ui/core/theme/themes.ts:176 (chrome value at :164)` | Unchanged; solarized is the only theme that hand-forks its terminal foreground. |  |
| updater-distribution/F5 | low | `open` | `build/installer.nsh:137` | Low held, but confidence is higher: this retire is NEW code (baseline had no step 3) and its own comments log it no-op'ing live twice — :47-56 and :126-133. Sam |  |
| updater-distribution/F7 | low | `open` | `scripts/update-manifests.mjs:71 (Installers block at :80-83)` | Unchanged. The checked-in manifest is now stale at PackageVersion 0.16.0 while docs/10 was bumped to v0.17.0 in the delta, so a regeneration is due anyway. |  |
| usage-metering/F5 | low | `open` | `src/main/usage.ts:260` | Unchanged. |  |
| usage-metering/F6 | low | `open` | `src/main/usage.ts:448` | Unchanged. Custody is fine — the value is still vault-encrypted before rest (usage-keys.ts:33-45) — so this stays an honesty/orphan-state gap, not an ADR 0008/0 |  |
| usage-metering/F7 | low | `open` | `bin/mogging.mjs:383` | The only bin/mogging.mjs change since c026463 is PROTOCOL_VERSION 10 -> 11 (line 25). Scope re-confirmed: `some` lets any real-fetch provider end the wait, so t |  |
| vault-custody/F3 | low | `open` | `src/main/event-bridge.ts:108` | File byte-identical to baseline; no new env-ref slot landed. Stays LOW (needs an all-uppercase secret pasted into the ref field) but grep confirms it is the onl |  |
| vault-custody/F4 | low | `open` | `src/main/service-keys.ts:18` | No consolidation in the delta; now 3 sites plus the contract at {2,40} vs 3 at {2,64}. Stays LOW: the silent-clear path needs the accepted ${NAME} form passed t |  |
| window-chrome-clipboard/F7 | low | `open` | `src/main/clipboard.ts:103` | Low stands. recordImage downscales oversize images to a 240px thumbnail before recording (:200-211), so the worst case is bounded at ~100 x 256KB per window per |  |
| winget-homebrew-install-manifests/F6 | low | `open` | `scripts/update-manifests.mjs:149` | Unchanged by the delta. Note line 149's `.replace(version, '#{version}')` is an unanchored first-occurrence replace on the filename — order-dependent, exactly t |  |
| wizard-first-run/F1 | low | `open` | `src/ui/features/wizard/index.ts:662` | Down from MEDIUM: next open() re-disposes (269, 739-740, 1392) so at most one generation leaks, and currentOpen() (237-238) fences late callbacks. Residue: stal |  |
| wizard-first-run/F4 | low | `open` | `docs/02-mvp-and-roadmap.md:142` | Line drifted 140 -> 142 from edits above; sentence unchanged. The revamp commits (883fbc6, 6f16fcc) strengthened the cd line rather than removing it, so the doc |  |
| wizard-first-run/F5 | low | `open` | `src/ui/components/folder-browser.ts:337` | Untouched by the 32-commit delta in every respect. |  |
| wizard-first-run/F6 | low | `open` | `src/ui/features/wizard/index.ts:1611` | Unchanged by the delta. Bounded to presets (no credential or launch path), so LOW stands; the unhandled rejection remains a second silent symptom. |  |
| workspace-tabs-switching-and-per-workspace-themes/F6 | low | `open` | `src/ui/features/workspace/controller.ts:465` | Unchanged by the delta. Stays low: the ghost role is metadata (pane-meta/daemon row plus one capped grant) and never types into a pane, so the we-type/user-exec |  |
| workspace-tabs-switching-and-per-workspace-themes/F7 | low | `open` | `src/ui/features/workspace/README.md:16` | Unchanged. Add the line-19 error to the original claim: the picker is src/ui/features/settings/theme-picker.ts, built at settings/index.ts:96 and mounted at :54 |  |
| agent-state-attention/F6 | none | `invalid` | `src/ui/core/attention/completions.ts:25-35` | Auditor misread the log's lifetime as surviving reload. Residual: the replayed entry is timestamped at reload time, but completions.ts:16-18 already declares th |  |
| ci-workflows/F6 | none | `fixed` | `.github/workflows/ci.yml:7` | Fixed on the ref's absence, not on the file changing — refs/stash no longer resolves, so it cannot be popped. Nothing unique lost: the WEIGHT gates landed at ci |  |
| per-pane-git-branch-dirty-chip-pipeline/F7 | none | `invalid` | `prompts/phase-2/03-per-pane-git.md:1` | Downgraded low->none: the premise is wrong, nothing is drifting. Small residual: the file is a build brief — grepping it for remote/poll/2.5/degrade/watch hits |  |
| persistence-restore/F4 | none | `fixed` | `src/pty-daemon/session.ts:480` | Fix landed at the restore-seed instead of the persist-cut as recommended — strictly better: it also cleans pre-fix rows, and RESTORE_MODE_RESET additionally cle |  |
