# 05 — Write-grant proof, the audit, docs, and the composed milestone

Read the pack README + the survey first. Final step: prove the whole, weaken
nothing.

## Goal
Close the pack the phase-tools way: the write boundary proven end-to-end on real
surfaces, every touched gate audited, docs/14 taught the bridge, and one composed
gate walking the promise — a global API key becoming safe, curated, verified
agent tools.

## Deliverables
1. **The write boundary, end-to-end**: in a live workspace whose grant is OFF, an
   agent pane's call to a `readOnly:false` bridge tool refuses with the sentence
   naming the switch; flipping the grant in Settings (the existing toggle — zero
   new UI) makes the SAME call land. MUTATIONRACE's single-fire law holds on that
   toggle unchanged (re-certify).
2. **The gate audit** (REPORT.md for the pack, phase-tools format): per touched
   gate — TOOLCARDS, TOOLPULSE, TOOLWHO, TOOLCRED, TOOLFIX, TOOLSMILESTONE,
   CONNPURE, CATSCHEMA/RESTSCHEMA, TOOLWORDS, SECRETFORMS, MUTATIONRACE — before /
   now / every moved assertion's new home, or the justified retirement line. Any
   unexplained shrinkage is a step failure. Stash-probe discipline on any
   suspicious red.
3. **docs/14 §**: "The REST bridge" under the tool-first opening — the curation
   law (and WHY: the survey's tool-explosion evidence, cited), the pinned-endpoint
   law, the write grant, custody unchanged, and the honest limits (a bridge tool
   is our curation of their API; the provider's own MCP, where good, stays
   preferred). `check-credential-wording.mjs` + DOCSREFS keep passing.
4. **Curation debt ledger**: any row still pending live verification is LISTED in
   REPORT.md with its operator instruction — dark rows never rot silently.
5. **Gate-count prose** re-mechanized for the pack's gates (RESTSCHEMA rides
   CATSCHEMA's row; RESTEXEC, RESTIMPORT, RESTCARDS, RESTMILESTONE are new rows);
   every counted file reconciled via `check-gate-count.mjs`.

## Gate — RESTMILESTONE (composed)
One env-gated smoke on the fixture REST API, in order, every arrow an assert:
fresh profile → the fixture family offers "Paste an API key" (family-level) → one
paste proves, vaults, connects every member (`✓ Connected · verified 0m ago`) →
the slot name reads saved on the CLI route (one-paste law) → identity lands
(`accountSource` asserted) → scope into a workspace from the detail → launch env
carries the bridge row; pre-launch verify stamps within budget → an agent-shaped
`tools/call` reads through the bridge (fixture asserts the pinned URL + injected
header) → a WRITE tool refuses (grant off) → grant flipped → the same call lands
→ the key is revoked fixture-side → attention raises within one beat; the card
reads Needs attention → re-paste heals it → disconnect deletes the credential and
the CLI-route slot survives (it is the user's, not the connection's). Bracket
red/green on the write-refusal assert.

## Guardrails
- Full targeted battery green before the commit; the full-sweep certification
  rows go to REPORT.md as operator steps (the standing convention).
- Perf: the bridge adds ZERO boot-path work (I7) — prove MILESTONE/PERCEPTION
  numbers unchanged on a quiet box.

## Done when
RESTMILESTONE green (write-refusal bracketed); the audit table complete with zero
unexplained shrinkage; docs/14 + REPORT.md landed; sweep green vs baseline.
