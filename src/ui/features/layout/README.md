# ui/features/layout (Phase 1)

Home for the split/grid layout tree: pane templates (1/2/4/6/…16), drag-resize,
focus management, and mapping layout nodes to `TerminalPane` instances. The
`terminal` feature will render *into* slots this feature provides, instead of
appending a single pane.
