# ui/features/command-blocks (Phase 2)

Home for the Warp-style command-block UI: collapsible per-command sections with
exit-code color, timestamps, and search. Driven by OSC 133 A/B/C/D boundaries
detected in `backend/features/agent-state` and delivered over a command-block
contract slice.
